# Native Codex custom endpoints

The native desktop operator may save a custom Responses endpoint and key, then choose **Codex · Custom endpoint** for an individual chat. Saving does not require a model or a successful compatibility check. Ordinary **Codex** retains its native configuration and sign-in. Both choices use native Codex app-server; a gateway that implements Chat Completions alone is insufficient.

Session create/update requests use `backend: "codex"` with `codex_provider: "default" | "custom"`; missing selection means `default`. Provider selection is returned in session detail and summary, preserved by forks, and locked once a native conversation starts. Each custom conversation retains its endpoint credential revision while its model can change within that endpoint. Saving or resetting the current endpoint does not interrupt existing work, replace ordinary account credentials, or move existing chats to the new endpoint.

The runtime catalog publishes separate `backends.codex.custom_provider` metadata. Model discovery excludes affirmative non-chat capabilities such as embeddings and image generation. Unfamiliar models remain selectable and can also be entered manually. Per-model `model_capabilities` report `compatibility` as `unverified`, `verified`, or `unsupported`, plus explicitly advertised reasoning settings. No reasoning effort is inferred from an unfamiliar model ID or a successful basic check. Empty per-model efforts mean no effort should be sent, not that clients should fall back to ordinary Codex defaults.

Custom turns explicitly replace native collaboration settings to clear an inherited or earlier reasoning effort, and disable automatic reasoning summaries. Each custom process launch also prepares a private catalog from the installed executable's offline model metadata. It preserves native tools and instructions while removing built-in reasoning defaults and selecting standard Responses instead of the internal Lite serializer. This does not remove the complete `reasoning` object from every native request: an endpoint must still accept the actual Responses request generated by its installed Codex version. Ordinary account turns retain their existing settings and catalog.

Administrative routes require native admin authentication, reject browser-origin requests, and disable response caching:

| Method | Route | Purpose |
| --- | --- | --- |
| GET | `/api/admin/codex/provider` | Saved endpoint identity and whether a key exists; never returns the key. |
| POST | `/api/admin/codex/provider/test` | Explicit, optional basic model compatibility check using native Codex. |
| GET | `/api/admin/codex/provider/models` | Refresh the saved endpoint's catalog; optional `session_id` selects a chat's retained endpoint. |
| POST | `/api/admin/codex/provider/models` | Discover models using entered endpoint credentials. |
| PUT | `/api/admin/codex/provider` | Save `base_url` and a freshly entered `api_key`; `model` is optional. |
| DELETE | `/api/admin/codex/provider` | Reset the current custom endpoint selection. |

A saved-model check accepts `{model, session_id?, credential_id?}` without resubmitting the key. A supplied credential revision must match the current endpoint or selected chat; stale requests fail with HTTP 409. A chat can check its retained endpoint after the current settings change or reset. For older clients, `/test` also accepts entered `{base_url, api_key, model?}`; omitting the model performs discovery only. Save remains independent of both operations.

HTTPS is required except for literal loopback HTTP. URLs cannot contain credentials, query strings, fragments, or a `/responses` or `/chat/completions` operation suffix. Inputs are bounded and validation errors never echo supplied values. Discovery does not follow redirects or forward provider keys to another host.

Credentials live in a private `codex-provider` directory under the administrative state directory. Mode-0600 credential records are bound to a normalized endpoint and retained by revision for existing conversations. They are not encrypted at rest. Native config and process arguments contain the dedicated environment-variable name, never the key. Normal OpenAI/ChatGPT credentials are not copied to the custom endpoint. The legacy `/api/admin/codex/auth/api-key` mutation is disabled; use the custom endpoint routes instead.

The basic compatibility check owns a separate native app-server with a fresh temporary `CODEX_HOME`, ephemeral auth/thread state, temporary logs/databases, disabled integrations, and empty turn environments. It asks native `update_plan` to complete one harmless step and handles one in-memory dynamic ping. The model must return the random token supplied only in that tool result, then repeat it in a subsequent turn on the same thread. Other dynamic and approval requests are declined. No workspace commands or model-generated scripts are dispatched by the server.

A successful result is **Basic compatibility checked**, with `scope: "isolated_native_tools_and_continuation"` and `checks` for `native_tool_call`, `tool_roundtrip`, and `continuation`. It does not certify every production tool schema, workspace tool, integration, or reasoning setting. A text-only response is inconclusive. Explicit protocol, model, or unsupported-parameter rejection reports `unsupported`; authentication, connectivity, and incomplete checks remain `unverified`. Observations are cached in memory per endpoint, key, and model and are preserved across catalog refreshes. A server restart clears these observations.

The total check budget is 45 seconds, followed by owned-process cleanup. Results use fixed, secret-safe messages and never expose raw provider errors. Checks do not save credentials or change a chat's provider. There are no automatic probe retries. Configured readiness means only that a local key exists; unknown models remain available for deliberate selection.

Use pure-module or AST-isolated tests and controlled localhost Responses fixtures. Do not import the server monolith or use production credentials to test this feature.
