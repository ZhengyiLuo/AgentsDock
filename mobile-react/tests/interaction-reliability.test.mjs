import fs from 'node:fs'
import path from 'node:path'
import test from 'node:test'
import assert from 'node:assert/strict'

const composer = fs.readFileSync(path.resolve('src/components/Composer.tsx'), 'utf8')
const store = fs.readFileSync(path.resolve('src/store/useAppStore.ts'), 'utf8')
const activityHealth = fs.readFileSync(path.resolve('src/lib/activity-health.ts'), 'utf8')
const cache = fs.readFileSync(path.resolve('src/storage/cache.ts'), 'utf8')
const dialogs = fs.readFileSync(path.resolve('src/components/Dialogs.tsx'), 'utf8')
const inspector = fs.readFileSync(path.resolve('src/components/Inspector.tsx'), 'utf8')
const terminal = fs.readFileSync(path.resolve('modules/agentsdock-native-terminal/ios/AgentsDockTerminalView.swift'), 'utf8')
const terminalModule = fs.readFileSync(path.resolve('modules/agentsdock-native-terminal/ios/AgentsDockNativeTerminalModule.swift'), 'utf8')
const terminalTypes = fs.readFileSync(path.resolve('modules/agentsdock-native-terminal/src/index.ts'), 'utf8')
const terminalView = fs.readFileSync(path.resolve('src/components/TerminalView.tsx'), 'utf8')
const appDelegateSubscriber = fs.readFileSync(path.resolve('modules/agentsdock-native-terminal/ios/AgentsDockKeyboardAppDelegateSubscriber.swift'), 'utf8')
const terminalModuleConfig = fs.readFileSync(path.resolve('modules/agentsdock-native-terminal/expo-module.config.json'), 'utf8')
const appShell = fs.readFileSync(path.resolve('src/components/AppShell.tsx'), 'utf8')
const appKeyboard = fs.readFileSync(path.resolve('src/lib/app-keyboard.ts'), 'utf8')
const keyboardGesture = fs.readFileSync(path.resolve('src/lib/keyboard-gesture.ts'), 'utf8')
const keyboardLifecycle = fs.readFileSync(path.resolve('src/lib/keyboard-lifecycle.ts'), 'utf8')
const keyboardControllerPatch = fs.readFileSync(path.resolve('patches/react-native-keyboard-controller@1.21.9.patch'), 'utf8')
const chatScreen = fs.readFileSync(path.resolve('src/components/ChatScreen.tsx'), 'utf8')
const timeline = fs.readFileSync(path.resolve('src/components/Timeline.tsx'), 'utf8')
const chatHeader = fs.readFileSync(path.resolve('src/components/ChatHeader.tsx'), 'utf8')
const ui = fs.readFileSync(path.resolve('src/components/ui.tsx'), 'utf8')
const sidebar = fs.readFileSync(path.resolve('src/components/Sidebar.tsx'), 'utf8')
const mediaGrid = fs.readFileSync(path.resolve('src/components/MediaGrid.tsx'), 'utf8')
const videoThumbnailLoader = fs.readFileSync(path.resolve('src/components/VideoThumbnailLoader.tsx'), 'utf8')
const artifactFileViewer = fs.readFileSync(path.resolve('src/components/file-viewer/ArtifactFileViewerModal.tsx'), 'utf8')
const artifactVideoPlayer = fs.readFileSync(path.resolve('src/components/file-viewer/ArtifactVideoPlayer.tsx'), 'utf8')
const artifactNativeVideoPlayer = fs.readFileSync(path.resolve('src/components/file-viewer/ArtifactNativeVideoPlayer.tsx'), 'utf8')
const artifactIOSSystemVideoPlayer = fs.readFileSync(path.resolve('src/components/file-viewer/ArtifactIOSSystemVideoPlayer.tsx'), 'utf8')
const artifactPlatformVideoPlayerAndroid = fs.readFileSync(path.resolve('src/components/file-viewer/ArtifactPlatformVideoPlayer.android.tsx'), 'utf8')
const nativeVideoModule = fs.readFileSync(path.resolve('modules/agentsdock-native-video/ios/AgentsDockNativeVideoModule.swift'), 'utf8')
const nativeVideoView = fs.readFileSync(path.resolve('modules/agentsdock-native-video/ios/AgentsDockNativeVideoView.swift'), 'utf8')
const nativeVideoTypes = fs.readFileSync(path.resolve('modules/agentsdock-native-video/src/index.ts'), 'utf8')
const nativeVideoModuleConfig = fs.readFileSync(path.resolve('modules/agentsdock-native-video/expo-module.config.json'), 'utf8')
const workspaceFileViewer = fs.readFileSync(path.resolve('src/components/file-viewer/WorkspaceFileViewerModal.tsx'), 'utf8')
const filePreview = fs.readFileSync(path.resolve('src/components/file-viewer/FilePreview.tsx'), 'utf8')
const mobileCodeEditor = fs.readFileSync(path.resolve('src/editor/MobileCodeEditor.tsx'), 'utf8')
const codeEditorEngine = fs.readFileSync(path.resolve('scripts/code-editor-entry.ts'), 'utf8')
const embedCodeEditor = fs.readFileSync(path.resolve('scripts/embed-code-editor.mjs'), 'utf8')
const codeEditorAssets = fs.readFileSync(path.resolve('src/editor/codeEditorAssets.ts'), 'utf8')
const workspaceFileEditing = fs.readFileSync(path.resolve('src/lib/workspace-file-editing.ts'), 'utf8')
const fileViewerHost = fs.readFileSync(path.resolve('src/components/file-viewer/FileViewerHost.tsx'), 'utf8')
const deferredLoadBoundary = fs.readFileSync(path.resolve('src/components/DeferredLoadBoundary.tsx'), 'utf8')
const deferredImport = fs.readFileSync(path.resolve('src/lib/deferred-import.ts'), 'utf8')
const agentServerClient = fs.readFileSync(path.resolve('src/api/AgentServerClient.ts'), 'utf8')
const mediaViewerState = fs.readFileSync(path.resolve('src/lib/media-viewer-state.ts'), 'utf8')
const fullscreenImageViewer = fs.readFileSync(path.resolve('src/components/FullscreenImageViewer.tsx'), 'utf8')
const fullscreenModalLayout = fs.readFileSync(path.resolve('src/lib/fullscreen-modal-layout.ts'), 'utf8')
const imageViewerGesture = fs.readFileSync(path.resolve('src/lib/image-viewer-gesture.ts'), 'utf8')
const format = fs.readFileSync(path.resolve('src/lib/format.ts'), 'utf8')
const timelineMemory = fs.readFileSync(path.resolve('src/lib/timeline-memory.ts'), 'utf8')
const timelineHistoryNavigation = fs.readFileSync(path.resolve('src/lib/timeline-history-navigation.ts'), 'utf8')
const timelineRowReuse = fs.readFileSync(path.resolve('src/lib/timeline-row-reuse.ts'), 'utf8')
const mobilePackage = JSON.parse(fs.readFileSync(path.resolve('package.json'), 'utf8'))
const composerInputSize = fs.readFileSync(path.resolve('src/lib/composer-input-size.ts'), 'utf8')
const composerToolbarLayout = fs.readFileSync(path.resolve('src/lib/composer-toolbar-layout.ts'), 'utf8')
const timelineProjection = fs.readFileSync(path.resolve('src/lib/timeline.ts'), 'utf8')
const timelineRows = fs.readFileSync(path.resolve('src/components/TimelineRows.tsx'), 'utf8')
const serverSearch = fs.readFileSync(path.resolve('src/lib/server-search.ts'), 'utf8')
const codexPermissionDefaults = fs.readFileSync(path.resolve('src/lib/codex-permissions.ts'), 'utf8')
const codexPermissionMenu = fs.readFileSync(path.resolve('src/components/CodexPermissionMenu.tsx'), 'utf8')

test('send, steer, and queued run-now controls retain touch-safe targets and durable busy gates', () => {
  assert.match(composer, /testID="chat-stop"/)
  assert.match(composer, /testID="chat-send-now"/)
  assert.match(composer, /testID="chat-send"/)
  assert.match(composer, /testID=\{`queued-send-now-\$\{turn\.queued_id\}`\}/)
  assert.match(composer, /accessibilityLabel=\{steerReviewsQueue \? 'Review queued messages to steer' : 'Steer current turn'\}/)
  assert.match(composer, /if \(steerReviewsQueue\)[\s\S]*?setQueueReviewRequest\(value => value \+ 1\)[\s\S]*?else void send\(true\)/)
  assert.match(composer, />Steer<\/Text>/)
  assert.match(composer, /accessibilityLabel=\{active \? "Steer with queued message" : "Run queued message now"\}/)
  assert.match(composer, />Run now<\/Text>/)
  assert.doesNotMatch(composer, /Send message now|Send queued message now|>Send now<\/Text>/)
  assert.match(composerToolbarLayout, /COMPOSER_TOOLBAR_TOUCH_SIZE = 44/)
  assert.match(composerToolbarLayout, /state\.active \? COMPOSER_DENSE_BACKEND_SLOT_WIDTH : COMPOSER_TOOLBAR_TOUCH_SIZE/)
  assert.match(composer, /runtimeMenuDense: \{ width: COMPOSER_TOOLBAR_TOUCH_SIZE, height: COMPOSER_TOOLBAR_TOUCH_SIZE/)
  assert.match(composer, /primaryButton: \{ flex: 1, minWidth: 44, minHeight: 44/)
  assert.match(composer, /testID="chat-primary-actions"[\s\S]*?setPrimaryActionsHeight\(measured\)/)
  assert.match(composer, /runNow: \{ minHeight: 44/)
  assert.match(store, /const sendPromptInFlight = new Set<string>\(\)/)
  assert.match(store, /const queuedRunInFlight = new Map<string, \{ queuedId: string; promise: Promise<boolean> \}>\(\)/)
  assert.match(store, /const stopTurnInFlight = new Set<string>\(\)/)
  assert.match(store, /sendingSessionIds: Set<string>/)
  assert.match(store, /stoppingSessionIds: Set<string>/)
  assert.match(store, /pendingQueuedRunIds: Set<string>/)
  assert.match(store, /queuedRunStatus: Record<string, QueuedRunStatus \| undefined>/)
  assert.match(composer, /testID="queued-run-status"/)
  assert.match(composer, /accessibilityRole="alert"/)
  assert.match(composer, /accessibilityLiveRegion="polite"/)
  assert.match(composer, /label="Dismiss queue status"/)
  assert.match(composer, /const hasReadyContent = Boolean\(draft\.trim\(\)\) \|\| \(!welcome && uploads\.length > 0\)/)
  assert.match(composer, /const sendDisabled = welcome \? false : \(networkDisabled \|\| pending\.length > 0 \|\| failed\.length > 0 \|\| sending/)
  assert.match(composer, /state\.stoppingSessionIds\.has\(sessionId\)/)
  assert.match(composer, /accessibilityState=\{\{ disabled: networkDisabled \|\| stopping, busy: stopping \}\}/)
  assert.match(store, /uploadPending\[sessionId\]\?\.length/)
  assert.match(store, /uploadFailed\[sessionId\]\?\.length/)
  assert.match(store, /if \(!prompt && !files\.length\) return false/)
  assert.match(store, /could not be resolved for steering/)
  assert.match(store, /const locallyObservedSeq = Math\.max\([\s\S]*?snapshots\[sessionId\]\?\.latestSeq[\s\S]*?latest_event_seq/)
  assert.match(store, /response\.event\.seq > locallyObservedSeq\) applyLiveEvent/)
  assert.match(store, /mergeTurnResponseSession\(response\.session, value\)/)
  assert.match(store, /locallyObservedSeq[\s\S]*?void get\(\)\.syncSelectedSession\('recovery'\)/)
  assert.match(composer, /disabled=\{networkDisabled \|\| queueBusy\} canEditAgent=\{canControlAgent\}/)
  assert.match(composer, /const blockedByEarlierDelivery = queuedTurnHasEarlierDeliveryBarrier\(allTurns, turn\.queued_id, asyncQueuedMessageControlsAvailable\(health\)\)/)
  assert.match(composer, /disabled=\{networkDisabled \|\| queueBusy \|\| blockedByEarlierDelivery\}/)
  assert.match(ui, /touchSize = 44/)
  assert.match(ui, /iconButton: \{ flexShrink: 0/)
  assert.match(sidebar, /header: \{ minHeight: 44/)
  assert.match(composer, /styles\.primaryButton, \{ backgroundColor: `\$\{colors\.red\}18`/)
  assert.match(composer, /<Square size=\{14\} color=\{colors\.red\} fill=\{colors\.red\}/)
  assert.match(composer, />\{stopping \? 'Stopping' : 'Stop'\}<\/Text>/)
})

test('large drafts collapse the composer immediately after send', () => {
  assert.match(composerInputSize, /if \(!draft\.length\) return COMPOSER_INPUT_MIN_HEIGHT/)
  assert.match(composerInputSize, /if \(!Number\.isFinite\(measuredHeight\)\) return COMPOSER_INPUT_MIN_HEIGHT/)
  assert.match(composerInputSize, /Math\.min\(\s*COMPOSER_INPUT_MAX_HEIGHT/)
  assert.match(composer, /const \[inputHeight, setInputHeight\] = useState\(COMPOSER_INPUT_MIN_HEIGHT\)/)
  assert.match(composer, /onContentSizeChange=\{event => setInputHeight\(measuredComposerInputHeight\(event\.nativeEvent\.contentSize\.height\)\)\}/)
  assert.match(composer, /const displayedInputHeight = Math\.min\(composerInputHeight\(draft, inputHeight\), viewportLimits\.inputMaxHeight, primaryInputBudget\)/)
  assert.match(composer, /height: displayedInputHeight, maxHeight: viewportLimits\.inputMaxHeight/)
  assert.match(composer, /useEffect\(\(\) => \{[\s\S]*?if \(!draft\.length\) setInputHeight\(COMPOSER_INPUT_MIN_HEIGHT\)[\s\S]*?\}, \[draft\.length\]\)/)
  assert.match(composer, /if \(!text\.length\) setInputHeight\(COMPOSER_INPUT_MIN_HEIGHT\)/)
  assert.doesNotMatch(composer, /if \(!draft\.length \|\| !text\.length\)/)
  assert.match(composer, /const inputRef = useRef<TextInput>\(null\)/)
  assert.match(composer, /const request = sendPrompt\([\s\S]*?sendingSessionIds\.has\(sessionId\)[\s\S]*?drafts\[sessionId\][\s\S]*?inputRef\.current\?\.clear\(\)[\s\S]*?const sent = await request/)
  assert.match(composer, /<TextInput[\s\S]*?ref=\{inputRef\}[\s\S]*?testID="chat-composer-input"/)
  assert.match(store, /let consumedDraft = false[\s\S]*?drafts = \{ \.\.\.state\.drafts, \[sessionId\]: '' \}[\s\S]*?if \(consumedDraft\) void saveCurrentWorkspace\(get\)[\s\S]*?await scope\.client\.sendTurn/)
  assert.match(store, /restoreFailedChatComposer\([\s\S]*?originalDraft,[\s\S]*?rawReferences,[\s\S]*?currentDraft,[\s\S]*?currentReferences,[\s\S]*?sessionId/)
})

test('new chat creation cannot silently discard a mobile tap or failed request', () => {
  assert.match(sidebar, /<IconButton icon=\{Plus\} disabled=\{workspaceAdopting \|\| needsServerSetup\} onPress=\{onNewChat\} label="New chat" testID="sidebar-new-chat"/)
  // The mobile plus button creates a chat directly — the old Create Chat dialog
  // and its inline folder browser are gone.
  assert.match(appShell, /onNewChat=\{\(\) => void quickNewChat\(\)\}/)
  assert.doesNotMatch(appShell, /NewChatDialog/)
  assert.doesNotMatch(dialogs, /export function NewChatDialog/)
  assert.doesNotMatch(dialogs, /EffortChoiceGrid/)
  // A tap cannot be dropped or double-fire: re-entrancy is guarded, and the new
  // chat only opens on the phone after the server confirms creation.
  assert.match(appShell, /const quickChatInFlight = useRef\(false\)/)
  assert.match(appShell, /const quickNewChat = useCallback\(async \(\) => \{[\s\S]*?if \(quickChatInFlight\.current\) return[\s\S]*?quickChatInFlight\.current = true/)
  assert.match(appShell, /const created = await useAppStore\.getState\(\)\.quickCreateSession\(profileGeneration\)[\s\S]*?if \(created\) \{[\s\S]*?trackEvent\('chat_created'\)[\s\S]*?if \(compact\) setMobileChatOpen\(true\)[\s\S]*?requestAnimationFrame\(dismissAppKeyboard\)/)
  assert.match(appShell, /finally \{[\s\S]*?quickChatInFlight\.current = false/)
  assert.doesNotMatch(dialogs, /DEFAULT_CODEX_PERMISSION_SETTINGS/)
  assert.match(codexPermissionDefaults, /codex_approval_policy: DEFAULT_CODEX_APPROVAL_POLICY/)
  assert.match(codexPermissionDefaults, /codex_sandbox_mode: DEFAULT_CODEX_SANDBOX_MODE/)
  assert.match(codexPermissionDefaults, /codex_permission_profile: DEFAULT_CODEX_PERMISSION_PROFILE/)
  assert.match(codexPermissionDefaults, /codex_approvals_reviewer: DEFAULT_CODEX_APPROVALS_REVIEWER/)
  // quickCreateSession reuses the configured runtime defaults and open chat's
  // location, safely falls back for stale models, and shares one scoped request.
  const quickCreateStart = store.indexOf('\n  quickCreateSession(expectedGeneration) {')
  assert.ok(quickCreateStart >= 0)
  const quickCreate = store.slice(quickCreateStart, store.indexOf('\n  setChatDefaults(patch) {'))
  assert.match(quickCreate, /const scope = validatedConnectionOrReport\(get, set, expectedGeneration\)[\s\S]*?if \(!scope\) return Promise\.resolve\(false\)/)
  assert.match(quickCreate, /if \(quickCreateSessionInFlight\?\.scope === scope\) return quickCreateSessionInFlight\.promise/)
  assert.match(quickCreate, /const backends = selectableChatBackends\(state\.health\)/)
  assert.match(quickCreate, /backends\.includes\(state\.chatDefaults\.backend\) \? state\.chatDefaults\.backend : \(backends\[0\] \?\? 'codex'\)/)
  assert.match(quickCreate, /if \(runtimeSelectionError\(state\.health, state\.runtime, backend, model \|\| null\)\) \{[\s\S]*?model = ''[\s\S]*?effort = ''/)
  assert.match(quickCreate, /const operation = get\(\)\.createSession\(\{[\s\S]*?title: 'New chat'[\s\S]*?\}, scope\.generation\)\.finally\(/)
  assert.match(quickCreate, /if \(quickCreateSessionInFlight\?\.promise === operation\) quickCreateSessionInFlight = null/)
  assert.match(quickCreate, /quickCreateSessionInFlight = \{ scope, promise: operation \}[\s\S]*?return operation/)
  // createSession keeps the same reliability contract, records the choices as
  // the reused defaults, and persists them.
  const createSession = store.slice(store.indexOf('  async createSession('), quickCreateStart)
  assert.match(createSession, /if \(!scope\) return false/)
  assert.match(createSession, /if \(!connectionIsCurrent\(scope\)\) return false/)
  assert.match(createSession, /chatDefaults: \{[\s\S]*?backend: input\.backend[\s\S]*?\}/)
  assert.match(createSession, /scheduleCurrentWorkspaceSave\(get\)/)
  assert.match(createSession, /selectSession\(session\.id, scope\.generation\)/)
  assert.match(createSession, /return true/)
  assert.match(createSession, /catch \(error\)[\s\S]*?return false/)
})

test('server bookkeeping packets update projections without churning the resident transcript', () => {
  assert.match(store, /const TIMELINE_INTERNAL_EVENT_TYPES = new Set\(\[/)
  for (const type of [
    'turn_queue_updated',
    'turn_queue_reordered',
    'turn_queue_paused',
    'turn_queue_delivery_fenced',
    'queue_snapshot',
    'subagent_state',
    'job_updated',
    'job_deleted',
    'claude_subagents_stopped',
  ]) assert.match(store, new RegExp(`'${type}'`))
  assert.match(store, /if \(timelineInternal && queuedTurns === snapshot\.queuedTurns\)/)
  assert.match(store, /const mergedEvents = timelineInternal \? snapshot\.events : mergeEvents/)
  assert.match(store, /if \(event\.type\.startsWith\('job_'\)\) void get\(\)\.refreshJobs\(\)/)
})

test('iOS keyboard avoidance keeps the composer toolbar above attachment pickers', () => {
  assert.match(chatScreen, /behavior=\{Platform\.OS === 'ios' \? 'padding' : Platform\.OS === 'android' \? 'height' : undefined\}/)
  assert.match(chatScreen, /keyboardVerticalOffset=\{Platform\.OS === 'ios' \? insets\.top : 0\}/)
  assert.match(chatScreen, /automaticOffset=\{Platform\.OS === 'android'\}/)
  assert.match(chatScreen, /state !== 'active'\) cancelHideFallback\(\)/)
  assert.match(chatScreen, /state === 'active' \|\| state === 'inactive' \|\| state === 'background'/)
  assert.match(chatScreen, /paddingBottom: Platform\.OS === 'ios'[\s\S]*?\? iosKeyboard\.avoidanceEnabled \? 0 : insets\.bottom/)
  assert.match(keyboardLifecycle, /if \(event\.status !== 'active'\)/)
  assert.match(keyboardLifecycle, /appActive: false, visible: false, avoidanceEnabled: false/)
})

test('chat search validates locally and dismisses its keyboard reliably', () => {
  const storeSearch = store.slice(
    store.indexOf('  async search(query, sessionId, expectedGeneration)'),
    store.indexOf('\n  async pinMessage(', store.indexOf('  async search(query, sessionId, expectedGeneration)')),
  )
  const dialogSearch = dialogs.slice(
    dialogs.indexOf('function ScopedSearchDialog('),
    dialogs.indexOf('\ninterface DigestDialogProps', dialogs.indexOf('function ScopedSearchDialog(')),
  )

  assert.match(serverSearch, /Array\.from\(clean\)\.length >= SERVER_SEARCH_MIN_CHARACTERS/)
  assert.match(sidebar, /const handleSearchChange = useCallback\(\(value: string\) => \{[\s\S]*?clearSearch\(\)[\s\S]*?setQuery\(value\)/)
  assert.match(sidebar, /const clean = serverSearchQuery\(query\)[\s\S]*?if \(!clean \|\| needsServerSetup\)/)
  assert.match(storeSearch, /const request = \+\+searchRequestRevision/)
  assert.match(storeSearch, /const clean = serverSearchQuery\(query\)/)
  assert.match(storeSearch, /set\(\{ searchResults: \[\], searchBusy: true, searchError: null \}\)/)
  assert.match(storeSearch, /request !== searchRequestRevision/)
  assert.match(storeSearch, /connectionIsCurrent\(scope\) && request === searchRequestRevision/)
  assert.doesNotMatch(storeSearch, /set\(\{ error: errorMessage/)
  assert.match(storeSearch, /searchError: errorMessage\(error\)/)
  assert.match(dialogSearch, /const clean = serverSearchQuery\(query\)[\s\S]*?if \(!clean\) \{ setResults\(\[\]\); setBusy\(false\); setSearchError\(null\); return \}/)
  assert.match(dialogSearch, /setResults\(\[\]\)[\s\S]*?const timer = setTimeout/)
  assert.match(dialogSearch, /onChangeText=\{value => \{ setResults\(\[\]\); setBusy\(false\); setSearchError\(null\); setQuery\(value\) \}\}/)
  assert.match(dialogSearch, /testID="search-dialog-retry"/)

  assert.match(sidebar, /testID="sidebar-search-input"/)
  assert.match(sidebar, /testID="sidebar-search-retry"/)
  assert.match(sidebar, /seekTimelineResult\(result, profileScope\.profileGeneration\)/)
  assert.match(sidebar, /if \(clean\) return rankSidebarSessions\(sessions, clean, matchedByContent\)\.map<Row>/)
  assert.match(sidebar, /orderedSessionSections\(sessions, folderOrder, true, true\)[\s\S]*?collapsed\.has\(folder\)/)
  assert.match(sidebar, /returnKeyType="search"/)
  assert.match(sidebar, /submitBehavior="blurAndSubmit"/)
  assert.match(sidebar, /onSubmitEditing=\{dismissSearchKeyboard\}/)
  assert.match(sidebar, /keyboardDismissMode="on-drag"/)
  assert.match(sidebar, /keyboardShouldPersistTaps="handled"/)
  assert.match(sidebar, /alwaysBounceVertical=\{Platform\.OS === 'ios'\}/)
  assert.match(sidebar, /onScrollBeginDrag=\{dismissSearchKeyboard\}/)
  assert.doesNotMatch(sidebar, /onTouchStart=\{dismissSearchKeyboard\}/)
  assert.match(sidebar, /icon=\{RefreshCw\}[\s\S]*?dismissSearchKeyboard\(\)/)
  assert.match(sidebar, /const createFolder = \(\) => \{[\s\S]*?dismissSearchKeyboard\(\)[\s\S]*?promptText\(/)
  assert.match(dialogSearch, /returnKeyType="search"/)
  assert.match(dialogSearch, /submitBehavior="blurAndSubmit"/)
  assert.match(dialogSearch, /onSubmitEditing=\{dismissAppKeyboard\}/)
  assert.match(dialogSearch, /keyboardDismissMode="on-drag"/)
  assert.match(dialogSearch, /keyboardShouldPersistTaps="handled"/)
  assert.match(dialogSearch, /onScrollBeginDrag=\{dismissAppKeyboard\}/)
  assert.doesNotMatch(dialogSearch, /onTouchStart=\{dismissAppKeyboard\}/)
  assert.match(dialogSearch, /await seekTimelineResult\(result, profileGeneration\)/)
  assert.match(dialogSearch, /const cancelOpeningResult = \(\) => \{[\s\S]*?cancelTimelineSeek\(\)/)
  assert.match(dialogSearch, /onClose=\{closeSearch\}/)
  assert.match(store, /const seekIntent = \+\+timelineSeekIntentEpoch[\s\S]*?seekIntent !== timelineSeekIntentEpoch/)
  assert.match(store, /cancelTimelineSeek\(\) \{[\s\S]*?timelineSeekIntentEpoch \+= 1[\s\S]*?historyPagingEpoch \+= 1/)
  assert.match(store, /Promise\.all\(\[[\s\S]*?timelineSeekPage\(scope, result\.session_id,[\s\S]*?before: result\.seq[\s\S]*?after: Math\.max\(0, result\.seq - 1\)/)
  assert.match(store, /detached: true,[\s\S]*?anchorEventId: result\.event_id,[\s\S]*?anchorSeq: result\.seq/)
  assert.match(timeline, /maintainVisibleContentPosition=\{timelineMaintainVisiblePosition\(Boolean\(activeHistoryWindow\?\.detached && searchTargetAlignmentLocked\)\)\}/)
  assert.match(timeline, /initialScrollIndex=\{activeHistoryWindow\?\.detached && seekTargetIndex >= 0 \? seekTargetIndex : undefined\}/)
  assert.match(timeline, /for \(const delay of \[0, 140, 420\]\)/)
  assert.match(timeline, /setTimeout\(\(\) => \{[\s\S]*?setSearchTargetAlignmentLocked\(false\)[\s\S]*?\}, 900\)/)
  assert.match(sidebar, /searchResult: sidebarContentResult\(session\.title, clean, contentResults\.get\(session\.id\)\)/)
})

test('native Codex steering stays active without a false stopped-turn card', () => {
  assert.match(timelineProjection, /export function isNativeSteerSupersession/)
  assert.match(timelineProjection, /isNativeSteerSupersession\(event\)[\s\S]*?promoteNativeSteerCommentary\(superseded\)[\s\S]*?superseded\.finishedAt \|\|= event\.ts[\s\S]*?continue/)
  assert.match(timelineProjection, /event\.steer_interrupted_run_id\?\.trim\(\)[\s\S]*?promoteNativeSteerCommentary\(predecessor\)/)
  assert.match(timelineProjection, /promotedCommentaryIds[\s\S]*?assistantIds\.has\(event\.id\)/)
  assert.match(store, /const nativeSteerSupersession = isNativeSteerSupersession\(event\)/)
  assert.match(store, /const terminalEvent = !silentImport && \['turn_finished', 'turn_stopped'\]\.includes\(event\.type\) && !nativeSteerSupersession/)
  assert.match(store, /activityHealth\.observe\(activityScope\(scope\), event\)/)
  assert.match(activityHealth, /isNativeSteerSupersession\(event\)\) return this\.health/)
  assert.match(activityHealth, /runId !== healthOwner\) return this\.health/)
  assert.match(store, /scheduleLiveSnapshotSave\([\s\S]*?next,[\s\S]*?terminalEvent/)
})

test('active Codex progress renders at the live edge with Mac-style trace details', () => {
  assert.match(timelineProjection, /kind: 'progress'/)
  assert.match(timelineRows, /row\.kind === 'progress'[\s\S]*?<ProgressRowView/)
  assert.match(timelineRows, /function ProgressRowView/)
  assert.match(timelineRows, /const commentary = row\.events\.filter\(event => event\.phase === 'commentary'\)[\s\S]*?commentary\.map\(\(event, index\)/)
  assert.match(timelineRows, /activeTraceProgressPreview\(event\)/)
  assert.match(timelineRows, /testID="trace-live-updates"/)
  assert.match(timelineRows, /event\.type === 'reasoning_summary'[\s\S]*?<MarkdownContent value=\{text\}/)
  assert.match(timelineRows, /toolCount: Math\.ceil\(toolEventCount \/ 2\)/)
  assert.match(timelineRows, /reasoningTracePreview\(latestThought\?\.text\)/)
  assert.match(timelineRows, /row\.continues \? 'Activity continues' : 'Reasoning trace'/)
  assert.match(timelineRows, /thinking \$\{thoughtCount === 1 \? 'summary' : 'summaries'\}/)
  assert.match(timelineRows, /const toggleLabel = open \? 'Hide details' : 'Show details'/)
  assert.match(timelineRows, /event\.phase === 'commentary' \? 'Agent update' : 'Thinking summary'/)
  assert.match(timelineRows, /codexLifecycleSemanticKey\(row\.event\)[\s\S]*?<CodexLifecycleRowView/)
  assert.match(timelineRows, /Context compacted/)
})

test('completed Codex traces page full detail without leaking stale chat results', () => {
  const traceRow = timelineRows.slice(
    timelineRows.indexOf('function mergeTraceEvents('),
    timelineRows.indexOf('\nfunction ProgressRowView(', timelineRows.indexOf('function mergeTraceEvents(')),
  )
  assert.match(timelineRows, /<TraceRowView row=\{row\} sessionId=\{sessionId\}/)
  assert.match(traceRow, /new Map\(\[\.\.\.first, \.\.\.second\]\.map\(event => \[event\.id, event\]\)\)/)
  assert.match(traceRow, /\.sort\(\(left, right\) => left\.seq - right\.seq\)/)
  assert.match(traceRow, /Math\.max\(\.\.\.row\.events\.map\(event => event\.seq\)\)/)
  assert.match(traceRow, /const merged = loadedEvents \? mergeTraceEvents\(row\.events, loadedEvents\) : row\.events[\s\S]*?traceEventsWithinRow\(row, merged\)\.filter\(event => \([\s\S]*?!promotedIds\.has\(event\.id\)/)
  assert.match(traceRow, /!\(row\.runActive \?\? row\.active\)/)
  assert.match(traceRow, /loadRunTrace\(sessionId, runId, traceAnchor, cursor, 160, profileGeneration\)/)
  assert.match(traceRow, /page\.next_after \?\? cursor[\s\S]*?page\.has_more && nextCursor > cursor/)
  assert.match(traceRow, /generation !== loadGeneration\.current[\s\S]*?current\.profileGeneration !== profileGeneration[\s\S]*?current\.selectedSessionId !== sessionId/)
  assert.match(traceRow, /\{runId \? <View style=\{styles\.traceDetailActions\}>/)
  assert.match(traceRow, /trace-show-more-/)
  assert.match(traceRow, /trace-show-less-/)
  assert.match(traceRow, /loadedEvents\.length \|\| detailLimited[\s\S]*?onPress=\{showLess\}/)
  assert.match(traceRow, /accessibilityRole="alert"[\s\S]*?\{loadError\}/)
})

test('Codex lifecycle rows collapse repeated automatic compaction within one provider turn', () => {
  assert.match(timelineProjection, /const codexLifecycleRows = new Map<string, SystemRow>\(\)/)
  assert.match(timelineProjection, /appendCodexLifecycleEvent\(event\)/)
  assert.match(timelineProjection, /export function codexLifecycleSemanticKey/)
  assert.match(timelineProjection, /event\.turn_id\?\.trim\(\)[\s\S]*?event\.item_id\?\.trim\(\)/)
  assert.match(timelineProjection, /existing\.seq = event\.seq[\s\S]*?existing\.event = event/)
  assert.match(timelineProjection, /'codex_goal_updated'[\s\S]*?'codex_goal_cleared'[\s\S]*?'codex_compaction_started'/)
})

test('timeline sync uses bounded semantic pages with validated legacy fallback', () => {
  assert.match(store, /const TAIL_LIMIT = 240/)
  assert.match(store, /const SEMANTIC_TAIL_LIMIT = 48/)
  assert.match(store, /const SEMANTIC_OLDER_LIMIT = 96/)
  assert.match(store, /const OLDER_MAX_REQUESTS = 6/)
  assert.match(store, /const fullTail = reason === 'manual' \|\| !existingAtStart/)
  assert.match(store, /pageMode: 'semantic'/)
  assert.match(store, /if \(semanticPage\.semantic_paging === true\)/)
  assert.match(store, /fullTailSemanticPaging = false[\s\S]*?limit: TAIL_LIMIT/)
  assert.match(store, /liveSnapshot\.nextBefore[\s\S]*?liveSnapshot\.events\[0\]!\.seq/)
  assert.match(store, /const loadedIds = new Set\(initialSnapshot\.events\.map\(event => event\.id\)\)/)
  assert.match(store, /page\.next_before[\s\S]*?page\.next_semantic_before/)
  assert.match(store, /pagingEpoch !== historyPagingEpoch/)
  assert.match(store, /streamLatestSeq = Math\.max\(streamLatestSeq, event\.seq\)[\s\S]*?if \(event\.type === 'raw_event'\) return[\s\S]*?applyLiveEvent/)
  assert.match(cache, /event\.type !== 'raw_event'/)
  assert.match(timelineMemory, /HISTORY_WINDOW_EVENT_LIMIT = 2_400/)
  assert.match(timelineMemory, /HISTORY_TIMELINE_CHARACTER_BUDGET = 16_000_000/)
  assert.match(timelineMemory, /events,[\s\S]*?'newest',[\s\S]*?HISTORY_WINDOW_EVENT_LIMIT/)
  assert.match(store, /scheduleCurrentWorkspaceSave\(get\)/)
  assert.match(store, /const WORKSPACE_SAVE_DEBOUNCE_MS = 350/)
  assert.match(store, /const sourceNamespace = scope\.namespace[\s\S]*?await saveCurrentWorkspace\(get\)[\s\S]*?scope\.namespaceAdopting = true/)
})

test('battery-critical work is foreground-bound, coalesced, and released', () => {
  assert.match(store, /const FOREGROUND_REFRESH_MS = 60_000/)
  assert.doesNotMatch(store, /BACKGROUND_REFRESH_MS|12_000/)
  assert.match(store, /if \(refreshSessionsInFlight\?\.scope === scope\) return refreshSessionsInFlight\.promise/)
  assert.match(store, /refreshSessionsInFlight = \{ scope, promise: operation \}/)
  assert.match(store, /if \(nextState !== 'active'\) \{[\s\S]*?stopForegroundRefreshTimer\(\)[\s\S]*?stopSelectedStream\(\)[\s\S]*?liveConnected: false/)
  assert.match(store, /if \(NativeAppState\.currentState !== 'active'\) \{[\s\S]*?syncStatus: get\(\)\.connected \? 'cached' : 'offline'[\s\S]*?return[\s\S]*?\}\n  const scope = captureConnection\(\)/)
  assert.match(store, /const LIVE_SNAPSHOT_SAVE_DEBOUNCE_MS = 2_000/)
  assert.match(store, /const terminalEvent = !silentImport && \['turn_finished', 'turn_stopped'\]\.includes\(event\.type\)/)
  assert.match(store, /scheduleLiveSnapshotSave\([\s\S]*?next,[\s\S]*?terminalEvent/)
  assert.match(store, /function setSnapshotQueue\([\s\S]*?scheduleLiveSnapshotSave\(scope, next, true\)/)
  assert.match(store, /async syncSelectedSession[\s\S]*?scheduleLiveSnapshotSave\(scope, next, true\)/)
  assert.match(store, /const READ_RECEIPT_DEBOUNCE_MS = 3_000/)
  assert.match(store, /if \(await Notifications\.setBadgeCountAsync\(count\)\) lastBadgeCount = count/)
  assert.match(format, /if \(!incoming\.length\) return current/)
  assert.match(timeline, /reuseStableTimelineRows\(stableRows\.current, projectedRows\)/)
  assert.match(timelineRowReuse, /sameReferences\(left\.events, right\.events\)/)

  const thumbnailPreview = mediaGrid.slice(
    mediaGrid.indexOf('function VideoThumbnailPreview('),
    mediaGrid.indexOf('\nfunction QueuedVideoThumbnailLoader('),
  )
  const queuedThumbnailLoader = mediaGrid.slice(
    mediaGrid.indexOf('function QueuedVideoThumbnailLoader('),
    mediaGrid.indexOf('\nfunction MediaViewer('),
  )
  const thumbnailLoader = videoThumbnailLoader
  assert.doesNotMatch(thumbnailPreview, /useVideoPlayer/)
  assert.doesNotMatch(thumbnailPreview, /NativeAppState\.addEventListener/)
  assert.match(thumbnailPreview, /if \(Platform\.OS === 'ios' \|\| !enabled \|\| failed\)/)
  assert.match(queuedThumbnailLoader, /NativeAppState\.addEventListener\('change', state => setAppActive\(state === 'active'\)\)/)
  assert.match(queuedThumbnailLoader, /if \(!appActive\)/)
  assert.match(mediaGrid, /const AUTOMATIC_VIDEO_THUMBNAIL_LIMIT = 4/)
  assert.match(mediaGrid, /const videoThumbnailQueue = new SerialWorkQueue\(\)/)
  assert.match(thumbnailLoader, /useVideoPlayer\(source/)
  assert.match(thumbnailLoader, /status === 'error'/)
  assert.match(thumbnailLoader, /maxWidth: 320, maxHeight: 180/)
  assert.match(thumbnailLoader, /preferredForwardBufferDuration: 2/)
  assert.match(thumbnailLoader, /setTimeout\(onFailure, 8_000\)/)

  assert.match(appShell, /\{modalScopeCurrent && terminal && selected && !isWelcomeSession\(selected\.id\) \? <Modal visible/)
  assert.match(terminal, /UIApplication\.didBecomeActiveNotification/)
  assert.match(terminal, /applicationWillResignActive[\s\S]*?suspendSocket\(\)/)
  assert.match(terminal, /private func suspendSocket\(\)[\s\S]*?reconnectWorkItem\?\.cancel\(\)[\s\S]*?socket = nil[\s\S]*?cancel\(with: \.goingAway/)
  assert.match(terminal, /public func setSocketURL[\s\S]*?let activeSocket = socket[\s\S]*?socket = nil[\s\S]*?activeSocket\?\.cancel\(with: \.goingAway[\s\S]*?resumeSocketIfPossible\(\)/)
  assert.match(terminal, /socket\.send\(message\)[\s\S]*?guard let self, self\.socket === socket else \{ return \}[\s\S]*?self\.handleSocketFailure\(error\)/)
  assert.match(terminal, /let delay = min\(30, 1\.2 \* pow\(2, Double\(min\(reconnectAttempt, 5\)\)\)\)/)
  assert.match(terminal, /UIApplication\.shared\.applicationState == \.active/)
})

test('paged history stays continuous with the live bottom under one native anchor', () => {
  assert.match(timelineProjection, /export function projectPresentableHistory/)
  assert.match(timelineProjection, /return rows\.length \|\| isVerifiedSilentHistory\(events\) \? rows : null/)
  assert.match(timeline, /const matchingHistoryWindow = historyWindow\?\.profileGeneration === profileGeneration/)
  assert.match(timeline, /projectPresentableHistory\(matchingHistoryWindow\.snapshot\.events, matchingHistoryWindow\.snapshot\.files\)/)
  assert.match(timeline, /const activeHistoryWindow = matchingHistoryWindow && projectedHistoryRows/)
  assert.match(timeline, /activeHistoryWindow\.detached[\s\S]*?activeHistoryWindow\.snapshot[\s\S]*?mergeHistoryWithLiveSnapshot/)
  assert.match(timeline, /mergeHistoryWithLiveSnapshot\(activeHistoryWindow\.snapshot, liveSnapshot\)/)
  assert.match(timeline, /snapshot\?\.events === activeHistoryWindow\.snapshot\.events[\s\S]*?snapshot\?\.files === activeHistoryWindow\.snapshot\.files[\s\S]*?\? projectedHistoryRows/)
  assert.match(timelineMemory, /export function mergeHistoryWithLiveSnapshot/)
  assert.match(timelineMemory, /events = mergeEvents\(history\.events, live\.events\)/)
  assert.match(timeline, /maintainVisibleContentPosition=\{timelineMaintainVisiblePosition\(Boolean\(activeHistoryWindow\?\.detached && searchTargetAlignmentLocked\)\)\}/)
  assert.match(timelineHistoryNavigation, /SEARCH_TIMELINE_MAINTAIN_VISIBLE_POSITION = \{[\s\S]*?disabled: true/)
  assert.match(timelineHistoryNavigation, /LIVE_TIMELINE_MAINTAIN_VISIBLE_POSITION = \{[\s\S]*?startRenderingFromBottom: true/)
  assert.doesNotMatch(timeline, /PagingAnchor|pagingAnchor|getFirstVisibleIndex|getLayout|scrollToOffset/)
  assert.match(timeline, /const latestLiveSeq = liveSnapshot\?\.latestSeq/)
  assert.match(timeline, /liveTimelineAdvanced\(previousLiveSeq, latestLiveSeq\)/)
  assert.match(timeline, /activeHistoryWindow\?\.detached[\s\S]*?olderRequestInFlight\.current[\s\S]*?!nearBottomRef\.current[\s\S]*?userScrolling\.current/)
  assert.match(timelineHistoryNavigation, /return Number\.isFinite\(nextSeq\) && nextSeq > previousSeq/)
  assert.match(timeline, /nearBottomRef\.current[\s\S]*?!userScrolling\.current[\s\S]*?scrollToEnd\(\{ animated: false \}\)/)
  assert.match(timeline, /const finishScrollInteraction = useCallback[\s\S]*?interactionToken !== scrollInteractionToken\.current[\s\S]*?userScrolling\.current = false/)
  assert.match(timeline, /pendingAutomaticOlder\.current[\s\S]*?void requestOlder\(\)[\s\S]*?return/)
  assert.match(timeline, /const handleScrollEndDrag = useCallback[\s\S]*?requestAnimationFrame\(\(\) => \{[\s\S]*?momentumInteractionToken\.current != null[\s\S]*?finishScrollInteraction\(interactionToken\)/)
  assert.match(timeline, /const handleMomentumScrollBegin = useCallback[\s\S]*?cancelAnimationFrame\(scrollSettleFrame\.current\)[\s\S]*?userScrolling\.current = true/)
  assert.match(timeline, /const handleMomentumScrollEnd = useCallback[\s\S]*?finishScrollInteraction\(interactionToken\)/)
  assert.match(timeline, /onScrollEndDrag=\{handleScrollEndDrag\}/)
  assert.match(timeline, /onMomentumScrollBegin=\{handleMomentumScrollBegin\}/)
  assert.match(timeline, /onMomentumScrollEnd=\{handleMomentumScrollEnd\}/)
  assert.match(timeline, /const MAX_RECYCLED_TIMELINE_ITEMS = 18/)
  assert.match(timeline, /maxItemsInRecyclePool=\{MAX_RECYCLED_TIMELINE_ITEMS\}/)
  assert.match(timeline, /extraData=\{`\$\{fontScale\}:\$\{viewportWidth\}`\}/)
  assert.doesNotMatch(timeline, /extraData=\{[^}]*history/)
  assert.match(timeline, /const handleScrollBeginDrag = useCallback[\s\S]*?event\.nativeEvent\.contentOffset\.y/)
  assert.match(timeline, /onScrollBeginDrag=\{handleScrollBeginDrag\}/)
  assert.match(timeline, /Beginning of chat/)
  assert.match(timeline, /const scrollToLatest = useCallback\(\(\) => \{[\s\S]*?scrollToEnd\(\{ animated: true \}\)/)
  const latestCallback = timeline.slice(
    timeline.indexOf('const scrollToLatest = useCallback'),
    timeline.indexOf('const handleLayout = useCallback'),
  )
  assert.match(latestCallback, /if \(activeHistoryWindow\)[\s\S]*?exitHistory\(\)[\s\S]*?scrollToEnd\(\{ animated: false \}\)/)
  assert.match(timeline, /shouldShowTimelineLatest\(rows\.length, nearBottom, Boolean\(activeHistoryWindow\?\.detached\)\)/)
  assert.match(timelineHistoryNavigation, /detachedHistory \|\| !nearBottom/)
  assert.match(timeline, /timelineTargetRowIndex\(rows,[\s\S]*?const alignSeekTarget = useCallback[\s\S]*?scrollToIndex\(\{ index, animated: false, viewPosition: 0\.5 \}\)/)
  assert.match(timeline, /if \(activeHistoryWindow\?\.detached\) \{[\s\S]*?keyboardAnchorToken\.current \+= 1[\s\S]*?keyboardBottomAnchor\.current = false[\s\S]*?return/)
  assert.match(timeline, /matchingHistoryWindow && !loadingOlder && !projectedHistoryRows\) exitHistory\(\)/)
  assert.doesNotMatch(store, /historyWindow: matchingWindow \?\? \{/)
  assert.match(store, /!currentWindow && !projectPresentableHistory\(collected, filesFromEvents\(collected\)\)/)
})

test('transparent modal backdrops are siblings instead of responder-wrapping controls', () => {
  assert.match(dialogs, /<View style=\{styles\.backdrop\}>\{dismissable \? <Pressable[\s\S]*?StyleSheet\.absoluteFill[\s\S]*?: <View style=\{StyleSheet\.absoluteFill\} \/>\}\{panel\}<\/View>/)
  assert.doesNotMatch(dialogs, /<Pressable style=\{styles\.backdrop\}[\s\S]{0,220}?\{panel\}/)
  assert.match(inspector, /<View style=\{styles\.modalBackdrop\}><Pressable[\s\S]*?StyleSheet\.absoluteFill/)
})

test('chat details presents compact full-width media newest first', () => {
  assert.match(inspector, /const snapshotFiles = useAppStore\(state => state\.snapshots\[sessionId\]\?\.files\)/)
  assert.match(inspector, /const newestFiles = useMemo\(\(\) => filesNewestFirst\(snapshotFiles \?\? \[\]\), \[snapshotFiles\]\)/)
  assert.match(inspector, /<MediaGrid files=\{newestFiles\} sessionId=\{sessionId\} compact/)
  assert.doesNotMatch(inspector, /snapshot\.files\]\.reverse\(\)/)
  assert.match(mediaGrid, /const orderedFiles = useMemo\(\(\) => filesNewestFirst\(files\), \[files\]\)/)
  assert.match(mediaGrid, /const media = useMemo\(\(\) => orderedFiles\.filter\(isMedia\), \[orderedFiles\]\)/)
  assert.match(mediaGrid, /const compactFiles = compact \? orderedFiles\.filter\(file => compactFileIds\.has\(file\.id\)\) : \[\]/)
  assert.match(mediaGrid, /compactFiles\.length \? \([\s\S]*?<View style=\{styles\.compactMediaList\}>[\s\S]*?compactFiles\.map\(file =>/)
  assert.match(mediaGrid, /if \(!isMedia\(file\)\) \{[\s\S]*?return <FileRow/)
  assert.match(mediaGrid, /testID=\{`compact-media-tile-\$\{file\.id\}`\}/)
  assert.match(mediaGrid, /compactMediaList: \{ width: '100%'/)
  assert.match(mediaGrid, /compactTile: \{ width: '100%', minHeight: 112/)
  assert.match(mediaGrid, /compactPreviewButton: \{ width: '38%', minWidth: 96, maxWidth: 148/)
  assert.match(mediaGrid, /const \[visibleMediaCount, setVisibleMediaCount\] = useState\(mediaLimit\)/)
  assert.match(mediaGrid, /setVisibleMediaCount\(mediaLimit\)[\s\S]*?setVisibleFileCount\(fileLimit\)/)
  assert.match(mediaGrid, /const visibleMedia = media\.slice\(0, visibleMediaCount\)/)
  assert.match(mediaViewerState, /export function nextBoundedVisibleCount/)
  const mediaOverflow = mediaGrid.slice(
    mediaGrid.indexOf('{media.length > mediaLimit ?'),
    mediaGrid.indexOf('{documents.length > fileLimit ?'),
  )
  assert.match(mediaOverflow, /setVisibleMediaCount\(value => nextBoundedVisibleCount\(value, media\.length, mediaLimit\)\)/)
  assert.match(mediaOverflow, /visibleMedia\.length >= media\.length \? 'Show fewer media' : `Show \$\{Math\.min\(mediaLimit/)
  assert.doesNotMatch(mediaOverflow, /setSelection/)
  assert.match(mediaGrid, /const \{ openArtifacts, viewerActive \} = useFileViewer\(\)/)
  assert.match(mediaGrid, /openArtifacts\(\{ sessionId, files: viewerFiles, initialId: fileId, ownerKey: thumbnailOwnerKey \}\)/)
  assert.doesNotMatch(mediaGrid, /<Modal/)
  assert.match(fileViewerHost, /activeRequest\?\.kind === 'artifacts'[\s\S]*?<ArtifactFileViewerModal/)
  assert.match(mediaGrid, /const thumbnailOwnerInstanceId = useId\(\)/)
  assert.match(mediaGrid, /thumbnailOwnerKey=\{`\$\{compact \? 'compact' : 'timeline'\}:\$\{ownerKey\}:\$\{thumbnailOwnerInstanceId\}`\}/)
  assert.match(mediaGrid, /useAutomaticVideoThumbnailClaims\([\s\S]*?thumbnailOwnerKey,[\s\S]*?shouldGenerateAutomaticVideoThumbnail\(Platform\.OS, viewerActive\)[\s\S]*?media\.filter\(isVideo\)\.map\(file => file\.id\)[\s\S]*?: \[\]/)
  assert.match(mediaGrid, /key=\{`\$\{connectionKey\}:\$\{sessionId\}:\$\{ownerKey\}:\$\{compact \? 'compact' : 'timeline'\}`\}/)
  assert.equal((timelineRows.match(/<MediaGrid files=\{row\.files\} sessionId=\{sessionId\} ownerKey=\{row\.key\}/g) ?? []).length, 2)
  assert.doesNotMatch(mediaGrid, /hiddenCount|hiddenBadge|hiddenText/)
})

test('file metadata paging exposes loading, retry, and exhaustion without duplicate requests', () => {
  assert.match(store, /const filePageInFlight = new Map<string, Promise<void>>\(\)/)
  assert.match(store, /const requestKey = `\$\{scope\.generation\}:\$\{namespace\}:\$\{sessionId\}`/)
  assert.match(store, /const existingRequest = filePageInFlight\.get\(requestKey\)[\s\S]*?if \(existingRequest\) return existingRequest/)
  assert.match(store, /const offset = append \? paging\?\.nextOffset \?\? snapshot\.files\.length : 0/)
  assert.match(store, /hasMore: page\.has_more,[\s\S]*?nextOffset,[\s\S]*?error: null/)
  assert.match(store, /if \(append && paging\?\.hasMore === false\) return Promise\.resolve\(\)/)
  assert.match(inspector, /const filePaging = useAppStore\(state => state\.filePaging\[sessionId\]\)/)
  assert.match(inspector, /testID="file-page-loading"/)
  assert.match(inspector, /testID="file-page-error"/)
  assert.match(inspector, /testID="file-page-retry"[\s\S]*?refreshFiles\(sessionId, filePaging\.retryAppend, profileGeneration\)/)
  assert.match(inspector, /testID="file-page-load-more"/)
  assert.match(inspector, /testID="file-page-exhausted"[\s\S]*?All files loaded\./)
})

test('global errors stay in layout instead of covering composer controls', () => {
  assert.match(appShell, /testID="global-error-slot"/)
  assert.match(appShell, /errorSlot: \{ flexShrink: 0/)
  assert.doesNotMatch(appShell, /error: \{ position: 'absolute'/)
  assert.match(appShell, /global-error-slot[\s\S]*?styles\.workspace/)
})

test('pending server updates expose a safe profile-scoped mobile escape hatch', () => {
  assert.match(agentServerClient, /serverUpdateStatus\(\): Promise<ServerUpdateStatus> \{ return this\.get\('\/api\/admin\/update'\) \}/)
  assert.match(agentServerClient, /cancelServerUpdate\(scheduleId: string\)[\s\S]*?this\.post\('\/api\/admin\/update\/cancel', \{ schedule_id: scheduleId \}\)/)
  assert.match(store, /serverUpdatePendingError\(error\)/)
  assert.match(store, /onServerError: error => \{[\s\S]*?serverUpdatePendingError\(error\)[\s\S]*?pendingServerUpdate: serverUpdateNotice\(next, state\.health\)/)
  assert.match(store, /pendingServerUpdate: updatePending[\s\S]*?serverUpdateNotice\(scope, state\.health\)/)
  assert.match(store, /cancelableServerUpdateScheduleId\(status\)/)
  assert.match(store, /code === 'server_update_changed'[\s\S]*?serverUpdateStatus\(\)/)
  assert.doesNotMatch(store, /code === 'server_update_changed'[\s\S]{0,900}?cancelServerUpdate\(/, 'a compare-and-swap failure must not replay cancellation')
  assert.match(appShell, /testID="error-cancel-server-update"/)
  assert.match(appShell, /accessibilityLabel="Cancel scheduled server update"/)
  assert.match(appShell, /accessibilityState=\{\{ disabled: cancelingServerUpdate, busy: cancelingServerUpdate \}\}/)
  assert.match(appShell, /onPress=\{\(\) => \{ void cancelCurrentServerUpdate\(\) \}\}/)
  assert.match(appShell, /icon=\{X\}[\s\S]*?onPress=\{clearError\} disabled=\{cancelingServerUpdate\}[\s\S]*?testID="error-dismiss"/)
  assert.match(appShell, /errorAction: \{ minHeight: 44/)
})

test('scheduled jobs expose durable newest-first previous-run history', () => {
  assert.match(timelineRows, /jobDisplaySelection\(row\.events\)/)
  assert.match(timelineRows, /testID=\{`job-previous-runs-\$\{loadableJobId\}`\}/)
  assert.match(timelineRows, /if \(next && !historyLoaded\) void loadHistory\(null, true\)/)
  assert.match(timelineRows, /loadJobRuns\(sessionId, loadableJobId, beforeSeq, 20, profileGeneration\)/)
  assert.match(timelineRows, /mergeJobHistoryEvents\(loadedHistory, bundledHistory\)/)
  assert.match(timelineRows, /\{canLoadHistory \? <View style=\{\[styles\.jobHistory/)
  assert.match(timelineRows, /jobRunIdentity\(event\) \?\? event\.id/)
  assert.match(timelineRows, /testID=\{`job-load-older-runs-\$\{loadableJobId\}`\}/)
  assert.match(timelineRows, />Load older runs<\/Text>/)
  assert.match(timelineRows, /historySupported === false/)
  assert.match(timelineProjection, /return \[\.\.\.byRun\.values\(\)\]\.sort\(\(left, right\) => right\.seq - left\.seq\)/)
  assert.match(store, /async loadJobRuns\(sessionId, jobId, beforeSeq = null, limit = 20, expectedGeneration\)/)
  assert.match(timelineRows, /const traceRunId = latestStatus\.label === 'Deferred'/)
  assert.match(timelineRows, /event\.run_id === traceRunId[\s\S]*?'reasoning_summary'[\s\S]*?'code_diff'/)
  assert.match(timelineRows, /latestRunTraceAnchor[\s\S]*?Math\.max\(\.\.\.runScoped\.map\(event => event\.seq\)\)/)
  assert.match(timelineRows, /anchorSeq=\{latestRunTraceAnchor\} includeCommentary/)
  assert.match(timelineRows, /\{latestRunTraceRow \? <TraceRowView row=\{latestRunTraceRow\}/)
  assert.doesNotMatch(timelineRows, /\{open && latestRunTraceRow \? <TraceRowView/)
  assert.match(timelineRows, /anchorSeq=\{event\.seq\} includeCommentary/)
})

test('scheduled-job cards use reliable touch targets and authoritative history IDs', () => {
  const jobRow = timelineRows.slice(
    timelineRows.indexOf('function JobRowView'),
    timelineRows.indexOf('function JobHistoryRun'),
  )
  const latestDisclosure = jobRow.slice(
    jobRow.indexOf('<Pressable\n      testID={`job-latest-status-'),
    jobRow.indexOf('</Pressable>') + '</Pressable>'.length,
  )
  assert.match(latestDisclosure, /style=\{styles\.jobDisclosure\}/)
  assert.match(latestDisclosure, /<View style=\{styles\.jobHeader\}>/)
  assert.match(latestDisclosure, /!open \? <Text[\s\S]*?\{presentation\.preview\}<\/Text> : null/)
  assert.doesNotMatch(latestDisclosure, /selectable/)
  assert.match(jobRow, /useRecyclingState\(false, \[profileGeneration, row\.key, sessionId\]\)/)
  assert.match(jobRow, /String\(event\.job_id \|\| event\.job\?\.id \|\| ''\)\.trim\(\)/)
  assert.doesNotMatch(jobRow, /row\.key\.replace\(\/\^job:\//)
  assert.match(jobRow, /if \(!loadableJobId \|\| historyLoading\) return/)
  assert.doesNotMatch(jobRow, /previousRunCount <= 0/)
  assert.match(inspector, /testID=\{`scheduled-job-card-\$\{job\.id\}`\}/)
  assert.match(inspector, /accessibilityLabel=\{`Edit scheduled job \$\{job\.title\}`\}/)
  assert.match(inspector, /onPress=\{\(\) => \{ if \(scopeIsCurrent\(\)\) onJob\(job\.id\) \}\} style=\{styles\.jobContent\}/)
  assert.match(inspector, /<\/Pressable>\s*<IconButton icon=\{Pencil\}/)
  assert.match(inspector, /jobContent: \{ minHeight: 44, width: '100%', minWidth: 0/)
  assert.match(inspector, /testID=\{`run-scheduled-job-\$\{job\.id\}`\}/)
  assert.match(inspector, /jobRunsInFlight\.current\.has\(jobId\)/)
  assert.match(inspector, /jobRunsInFlight = useRef\(new Map<string, symbol>\(\)\)/)
  assert.match(inspector, /jobRunsInFlight\.current\.set\(jobId, token\)/)
  assert.match(inspector, /jobRunsInFlight\.current\.get\(jobId\) === token/)
  assert.match(inspector, /jobRunsInFlight\.current\.delete\(jobId\)/)
  assert.match(inspector, /const actionLocked = requestingRun \|\| pendingOnServer/)
  assert.match(inspector, /const runDisabled = actionLocked \|\| jobRunning \|\| Boolean\(runtimeError\)/)
  assert.match(inspector, /accessibilityState=\{\{ busy: requestingRun \|\| pendingOnServer, disabled: runDisabled \}\}/)
  assert.match(inspector, /<IconButton icon=\{Pencil\} size=\{14\} disabled=\{actionLocked\}/)
  assert.match(inspector, /<IconButton icon=\{Trash2\} size=\{14\} disabled=\{actionLocked\}/)
  assert.match(inspector, /testID=\{`run-scheduled-job-status-\$\{job\.id\}`\}/)
  assert.match(inspector, /accessibilityRole="alert"/)
  assert.match(inspector, /accessibilityLiveRegion="polite"/)
  assert.match(inspector, /AccessibilityInfo\.announceForAccessibility\(feedback\.message\)/)
  assert.match(inspector, /manual_run_pending === true/)
  assert.match(store, /const scheduledJobRunInFlight = new Map/)
  assert.match(store, /pendingJobRunIds: Set<string>/)
  assert.match(store, /const existing = scheduledJobRunInFlight\.get\(requestKey\)[\s\S]*?if \(existing\?\.scope === scope\) return existing\.promise/)
  assert.match(store, /async runJob\(jobId, expectedGeneration\)[\s\S]*?markJobsMutated\(scope\)[\s\S]*?void get\(\)\.refreshJobs\(scope\.generation\)/)
})

test('scheduled-job interval presets keep 10m and 30m taps out of nested scroll responders', () => {
  const intervalControls = dialogs.slice(
    dialogs.indexOf('<Label text="Interval" />'),
    dialogs.indexOf('<Label text="Custom seconds" />'),
  )
  assert.match(intervalControls, /<View testID="job-interval-presets" style=\{styles\.presetRow\}>/)
  assert.doesNotMatch(intervalControls, /<ScrollView/)
  assert.match(intervalControls, /testID=\{`job-interval-preset-\$\{value\}`\}/)
  assert.match(intervalControls, /accessibilityState=\{\{ selected \}\}/)
  assert.match(intervalControls, /onPress=\{\(\) => selectIntervalPreset\(value\)\}/)
  assert.match(dialogs, /const selectIntervalPreset = \(value: number\) => \{\s*setIntervalValue\(String\(value\)\)\s*dismissAppKeyboard\(\)\s*\}/)
  assert.match(dialogs, /preset: \{ minWidth: 52, minHeight: 44, flexBasis: '22%', flexGrow: 1/)
})

test('scheduled-job native pickers preserve and synchronously submit the chosen wall time', () => {
  assert.match(dialogs, /from '@expo\/ui\/community\/datetime-picker'/)
  assert.match(dialogs, /testID="job-start-mode-time"/)
  assert.match(dialogs, /testID="job-start-picker"/)
  assert.match(dialogs, /testID="job-start-date-picker"/)
  assert.match(dialogs, /testID="job-start-time-picker"/)
  assert.match(dialogs, /testID="job-start-date-button"/)
  assert.match(dialogs, /testID="job-start-time-button"/)
  assert.match(dialogs, /androidStartPicker === 'date' \? scheduleWallTimeAndroidDatePickerDate/)
  assert.match(dialogs, /applyPickerValue\(part, value, part === 'date'\)[\s\S]*?setAndroidStartPicker\(null\)/)
  assert.match(dialogs, /onDismiss=\{\(\) => setAndroidStartPicker\(null\)\}/)
  assert.match(dialogs, /mode=\{androidStartPicker\}/)
  const updateStart = dialogs.slice(dialogs.indexOf('  const updateStart ='), dialogs.indexOf('  const selectStartMode ='))
  assert.ok(updateStart.indexOf('startRef.current = value') < updateStart.indexOf('setStart(value)'), 'picker callbacks must update the submission ref before React state')
  const selectStartMode = dialogs.slice(dialogs.indexOf('  const selectStartMode ='), dialogs.indexOf('  const pickerDate ='))
  assert.ok(selectStartMode.indexOf('startModeRef.current = value') < selectStartMode.indexOf('setStartMode(value)'), 'mode taps must update the submission ref before React state')
  assert.match(dialogs, /const submittedStartMode = startModeRef\.current/)
  assert.match(dialogs, /if \(submittedStartMode === 'immediate'\)/)
  assert.match(dialogs, /scheduledTime = parseScheduleWallTime\(startRef\.current, scheduleKind\)/)
  assert.match(dialogs, /first_run_at: scheduledTime/)
  assert.match(dialogs, /Object\.assign\(patch, jobNextRunUpdatePatch\(\{[\s\S]*?mode: submittedStartMode,[\s\S]*?scheduledTime,[\s\S]*?\}\)\)/)
  assert.match(dialogs, /keyboardShouldPersistTaps="always"/)
})

test('chat-details actions use an exactly-once iOS dismissal handoff with a guarded fallback', () => {
  assert.match(appShell, /const IOS_INSPECTOR_HANDOFF_FALLBACK_MS = 1_000/)
  assert.match(appShell, /const pendingInspectorAction = useRef<InspectorAction \| null>\(null\)/)
  assert.match(appShell, /const pendingInspectorFallback = useRef<ReturnType<typeof setTimeout> \| null>\(null\)/)
  assert.match(appShell, /const finishOptionsDismissal = useCallback\(\(\) => \{[\s\S]*?clearInspectorFallback\(\)[\s\S]*?const action = pendingInspectorAction\.current[\s\S]*?pendingInspectorAction\.current = null[\s\S]*?if \(action\) openInspectorAction\(action\.kind, action\.jobId\)/)
  assert.match(appShell, /pendingInspectorAction\.current = \{ kind, jobId \}[\s\S]*?closeOptions\(\)[\s\S]*?Platform\.OS !== 'ios'[\s\S]*?requestAnimationFrame\(finishOptionsDismissal\)[\s\S]*?pendingInspectorFallback\.current = setTimeout\(\(\) => \{[\s\S]*?pendingInspectorFallback\.current = null[\s\S]*?finishOptionsDismissal\(\)[\s\S]*?IOS_INSPECTOR_HANDOFF_FALLBACK_MS/)
  assert.match(appShell, /clearInspectorFallback\(\)[\s\S]*?pendingInspectorAction\.current = null[\s\S]*?setModalGeneration\(profileGeneration\)/)
  assert.match(appShell, /useEffect\(\(\) => clearInspectorFallback, \[clearInspectorFallback\]\)/)
  assert.match(appShell, /onDismiss=\{finishOptionsDismissal\}/)
  for (const action of ['digest', 'terminal', 'processes', 'tmux']) {
    assert.ok(appShell.includes(`queueInspectorAction('${action}')`), `${action} must use the dismissal handoff`)
  }
  assert.match(appShell, /onJob=\{jobId => queueInspectorAction\('job', jobId\)\}/)
})

test('timeline uses fixed prepend anchoring and resets recycled disclosure state', () => {
  assert.equal(mobilePackage.dependencies['@shopify/flash-list'], '2.3.2')
  assert.match(timelineRows, /import \{ useRecyclingState \} from '@shopify\/flash-list'/)
  assert.match(timelineRows, /const \[expanded, setExpanded\] = useRecyclingState\(false, \[row\.key\]\)/)
  assert.match(timelineRows, /const \[open, setOpen\] = useRecyclingState\(false, \[row\.key\]\)/)
  assert.match(timelineRows, /const \[showHistory, setShowHistory\] = useRecyclingState\(false, \[profileGeneration, row\.key, sessionId\]\)/)
  assert.match(timelineRows, /testID=\{`trace-live-update-\$\{event\.id\}`\}/)
  assert.match(timelineRows, /first=\{index === 0\}/)
  assert.doesNotMatch(timelineRows, /testID=\{`trace-live-update-\$\{index\}`\}/)
})

test('long-chat scrolling bounds native work without weakening anchors or live traces', () => {
  assert.match(timeline, /const TIMELINE_DRAW_DISTANCE = 250/)
  assert.match(timeline, /drawDistance=\{TIMELINE_DRAW_DISTANCE\}/)
  assert.match(timeline, /const handleScroll = useCallback\([\s\S]*?if \(nearBottomRef\.current !== next\) \{[\s\S]*?setNearBottom\(next\)/)
  assert.doesNotMatch(timeline, /setNearBottom\(current => current === next \? current : next\)/)
  assert.match(timeline, /const requestOlder = useCallback[\s\S]*?\}, \[canLoadOlder, loadOlder, loadingOlder, sessionId\]\)/)
  assert.match(timeline, /onScrollBeginDrag=\{handleScrollBeginDrag\}/)
  assert.match(timeline, /onScrollEndDrag=\{handleScrollEndDrag\}/)
  assert.match(timeline, /onMomentumScrollBegin=\{handleMomentumScrollBegin\}/)
  assert.match(timeline, /onMomentumScrollEnd=\{handleMomentumScrollEnd\}/)
  assert.match(timeline, /if \(row\.kind === 'message'\) return `message:\$\{row\.role\}`/)
  assert.match(timeline, /if \(codexLifecycleSemanticKey\(row\.event\)\) return 'system:lifecycle'/)
  assert.match(timelineRows, /const MESSAGE_PREVIEW_CHARACTER_LIMIT = 3_200/)
  assert.match(timelineRows, /foldMarkdownSource\(full, MESSAGE_PREVIEW_CHARACTER_LIMIT\)/)
  assert.match(timelineRows, /const detailWindow = useMemo\(\(\) => \{\s*if \(!open\) return null/)
  assert.match(timeline, /maintainVisibleContentPosition=\{timelineMaintainVisiblePosition\(Boolean\(activeHistoryWindow\?\.detached && searchTargetAlignmentLocked\)\)\}/)
  assert.match(timelineRows, /commentary\.map\(\(event, index\) => <ProgressLineView/)
})

test('compact back navigation cannot leave an animated pane over the selector', () => {
  const endHandler = appShell.match(/\.onEnd\(event => \{([\s\S]*?)\n    \}\)\n    \.onTouchesCancelled/)?.[1] ?? ''
  assert.match(endHandler, /if \(shouldClose\) \{[\s\S]*?translateX\.value = width[\s\S]*?runOnJS\(onClose\)\(\)[\s\S]*?return/)
  assert.doesNotMatch(endHandler, /finished/)
  assert.doesNotMatch(endHandler, /withTiming\(shouldClose/)
})

test('separate controls offer guarded attachments and Mac-parity quick messages', () => {
  assert.match(composer, /from 'expo-image-picker'/)
  assert.match(composer, /from '@expo\/ui\/community\/menu'/)
  assert.match(composer, /const QUICK_MESSAGES = \['Status report', 'Keep going\.', 'Verify the result carefully\.'\] as const/)
  assert.match(composer, /const options = \['Reference another chat', 'Photo Library', 'Files', 'Reference a server \(@@\)', 'Cancel'\]/)
  assert.match(composer, /const QUICK_MESSAGE_ACTIONS: MenuAction\[\] = QUICK_MESSAGES\.map\(\(title, index\) => \(\{[\s\S]*?id: `quick-message:\$\{index\}`,[\s\S]*?title,/)
  assert.match(composer, /actions=\{QUICK_MESSAGE_ACTIONS\}/)
  assert.match(composer, /event\.nativeEvent\.event\.startsWith\(prefix\)/)
  assert.match(composer, /const phrase = QUICK_MESSAGES\[Number\(event\.nativeEvent\.event\.slice\(prefix\.length\)\)\]/)
  assert.match(composer, /void send\(false, phrase, false\)/)
  assert.match(composer, /title="Quick messages"/)
  assert.match(composer, /ImagePicker\.launchImageLibraryAsync\(\{[\s\S]*?mediaTypes: \['images'\][\s\S]*?allowsMultipleSelection: true[\s\S]*?selectionLimit: 20/)
  assert.match(composer, /DocumentPicker\.getDocumentAsync\(\{ multiple: true, copyToCacheDirectory: true \}\)/)
  assert.match(composer, /photoAssetsToUploads\(result\.assets\)/)
  assert.match(composer, /const ATTACHMENT_PICKER_SEND_GUARD_MS = 600/)
  assert.match(composer, /attachmentSendGuardedRef\.current = true/)
  assert.match(composer, /const currentSendDisabled = networkDisabled[\s\S]*?\|\| attachmentSendGuardedRef\.current[\s\S]*?if \(consumeComposer && \(currentSendDisabled \|\| !currentHasReadyContent\)\) return/)
  assert.match(composer, /guardSendAfterPicker\(\)[\s\S]*?void attachFiles/)
  assert.match(composer, /testID="chat-attach"/)
  assert.match(composer, /icon=\{Paperclip\}[\s\S]*?label="Add files, photos, or another chat"/)
  assert.match(composer, /testID="chat-quick-messages"[\s\S]*?accessibilityLabel="Quick messages"/)
  assert.match(composer, /<MessageCircleMore size=\{19\} color=\{colors\.muted\}/)
  assert.match(composer, /quickMessageDisabled = networkDisabled \|\| sending/)
  assert.match(composer, /quickMessages: \{ width: COMPOSER_TOOLBAR_TOUCH_SIZE, height: COMPOSER_TOOLBAR_TOUCH_SIZE, flexShrink: 0/)
  assert.match(composerToolbarLayout, /COMPOSER_WIDE_CONTROLS_MIN_WIDTH = 600/)
  assert.match(composer, /const compactToolbar = composerWidth === 0 \|\| isCompactComposerToolbar\(composerWidth\)/)
  assert.match(composer, /onLayout=\{event => \{[\s\S]*?Math\.floor\(event\.nativeEvent\.layout\.width\)[\s\S]*?setComposerWidth/)
  assert.match(composer, /\{!compactToolbar \? <>[\s\S]*?<Pill tone="neutral">/)
  assert.match(composer, /<CodexPermissionMenu sessionId=\{sessionId\} compact=\{compactToolbar\}/)
  assert.match(composer, /active \|\| constrainedKeyboard \? <View testID="chat-primary-actions"/)
  assert.match(composer, /!constrainedKeyboard \? <View style=\{\[styles\.toolbar/)
  assert.match(composer, /testID="chat-hide-keyboard"/)
  assert.match(composer, /constrainedKeyboard \? 1 : undefined\}[\s\S]*?>Steer<\/Text>/)
  assert.doesNotMatch(composer, /COMPOSER_STEER_LABEL_MIN_WIDTH/)
  assert.match(composer, /primaryActionsConstrained: \{ minHeight: 44, paddingBottom: 0 \}/)
  assert.match(codexPermissionMenu, /trigger: \{ height: COMPOSER_TOOLBAR_TOUCH_SIZE, flexShrink: 0/)
  assert.match(codexPermissionMenu, /triggerCompact: \{ width: COMPOSER_TOOLBAR_TOUCH_SIZE \}/)
  assert.match(composerToolbarLayout, /COMPOSER_PERMISSION_FACE_SIZE = 28/)
  assert.match(composerToolbarLayout, /COMPOSER_STOP_FACE_SIZE = 30/)
  assert.match(composerToolbarLayout, /COMPOSER_SEND_FACE_SIZE = 32/)
  assert.match(codexPermissionMenu, /triggerCompactFace: \{ width: COMPOSER_PERMISSION_FACE_SIZE, height: COMPOSER_PERMISSION_FACE_SIZE/)
  assert.match(composer, /idleSend: \{ flex: 0, minWidth: 82/)
  assert.match(codexPermissionMenu, /backgroundColor: compact \? 'transparent' : colors\.raised/)
  assert.match(composer, /<BackendMark backend=\{backend\}[\s\S]*?\{!welcome \? quickMessageControl : null\}[\s\S]*?\{!welcome && backend === 'codex' \? <CodexPermissionMenu sessionId=\{sessionId\}[\s\S]*?<View style=\{styles\.toolbarSpacer\}/)
  assert.match(composer, /sendPrompt\(steer, profileGeneration, sessionId, \{[\s\S]*?promptOverride,[\s\S]*?consumeComposer,[\s\S]*?admissionToken,[\s\S]*?\}\)/)
  assert.match(store, /const consumeComposer = options\?\.consumeComposer !== false/)
  assert.match(store, /const prompt = \(options\?\.promptOverride \?\? originalDraft\)\.trim\(\)/)
  assert.match(store, /const files = consumeComposer \? options\?\.admittedFiles \?\? get\(\)\.uploads\[sessionId\] \?\? \[\] : \[\]/)
  assert.match(composer, /const attachmentDisabled = networkDisabled \|\| pickingAttachment \|\| admissionPreflight/)
  assert.match(store, /if \([\s\S]*?consumeComposer[\s\S]*?&& state\.drafts\[sessionId\] === originalDraft[\s\S]*?&& chatReferencesEqual\(state\.chatReferencesBySession\[sessionId\] \?\? \[\], rawReferences\)[\s\S]*?\) \{/)
  assert.match(store, /uploads: consumeComposer/)
  assert.match(composer, /import \{ Image \} from 'expo-image'/)
  assert.match(composer, /horizontal[\s\S]*?testID="attachment-shelf"/)
  assert.match(composer, /image && connectionReady \? \{ uri: client\.fileURL\(sessionId, file\.id\), headers: client\.authHeaders\(\) \} : null/)
  assert.match(composer, /source = image \? \{ uri: file\.uri \} : null/)
  assert.match(composer, /fileIconWell: \{ width: 48, height: 48, minWidth: 48, flexShrink: 0/)
  assert.match(composer, /Upload failed · Tap to retry/)
  assert.match(composer, /void attachFiles\(\[file\], profileGeneration, sessionId\)/)
  assert.match(composer, /const closePreview = useCallback\(\(\) => \{[\s\S]*?setPreview\(null\)[\s\S]*?requestAnimationFrame\(dismissAppKeyboard\)[\s\S]*?\}, \[\]\)/)
  assert.match(composer, /const openPreview = useCallback\(\(name: string, source: AttachmentImageSource\) => \{[\s\S]*?setPreview\(\{ name, source \}\)[\s\S]*?requestAnimationFrame\(dismissAppKeyboard\)/)
  assert.match(composer, /visible=\{preview != null\}[\s\S]*?onRequestClose=\{closePreview\}/)
  assert.match(composer, /onAccessibilityEscape=\{closePreview\}/)
  assert.match(composer, /testID="attachment-preview-close"/)
  assert.match(composer, /testID="attachment-image-dismiss-surface"[\s\S]*?gestureTestID="attachment-image-dismiss-gesture"/)
  assert.match(composer, /const insets = useSafeAreaInsets\(\)/)
  assert.match(composer, /<View onAccessibilityEscape=\{closePreview\}[\s\S]*?fullscreenModalPadding\(insets, Platform\.OS\)/)
  assert.doesNotMatch(composer, /<SafeAreaView onAccessibilityEscape=\{closePreview\}/)
  assert.match(composer, /styles\.previewHeader[\s\S]*?testID="attachment-preview-close"[\s\S]*?styles\.previewTitle/)
  assert.match(composer, /previewHeader: \{ minHeight: FULLSCREEN_HEADER_MIN_HEIGHT,[^}]*paddingHorizontal: 12, paddingVertical: FULLSCREEN_HEADER_GUTTER/)
})

test('fullscreen media viewers keep close outside bounded image zoom and dismissal gestures', () => {
  assert.match(artifactFileViewer, /presentationStyle="fullScreen"/)
  assert.match(artifactFileViewer, /onAccessibilityEscape=\{onClose\}/)
  assert.match(fileViewerHost, /const modalInsets = useSafeAreaInsets\(\)/)
  assert.match(fileViewerHost, /<ArtifactFileViewerModal request=\{activeRequest\} modalInsets=\{modalInsets\}/)
  assert.match(fileViewerHost, /<WorkspaceFileViewerModal request=\{activeRequest\} modalInsets=\{modalInsets\}/)
  assert.match(artifactFileViewer, /fullscreenModalPadding\(modalInsets, Platform\.OS\)/)
  assert.match(workspaceFileViewer, /fullscreenModalPadding\(modalInsets, Platform\.OS\)/)
  assert.doesNotMatch(artifactFileViewer, /<SafeAreaView/)
  assert.doesNotMatch(workspaceFileViewer, /<SafeAreaView/)
  assert.match(artifactFileViewer, /<SheetCloseButton onPress=\{onClose\} label="Close file viewer" testID="artifact-file-viewer-close" activateOnPressIn/)
  assert.match(artifactFileViewer, /collapsable=\{false\} pointerEvents="auto" style=\{\[styles\.header/)
  assert.match(artifactFileViewer, /testID: 'artifact-image-dismiss-surface',[\s\S]*?gestureTestID: 'artifact-image-dismiss-gesture'/)
  assert.match(filePreview, /<SwipeDismissImage[\s\S]*?testID=\{imageViewer\?\.testID \?\? 'file-preview-image-zoom-surface'\}[\s\S]*?<Image/)
  assert.match(artifactNativeVideoPlayer, /testID="artifact-video-dismiss-surface" gestureTestID="artifact-video-dismiss-gesture"/)
  assert.match(artifactNativeVideoPlayer, /loadProblem \? <View accessibilityRole="alert" testID="artifact-video-problem"/)
  assert.match(artifactNativeVideoPlayer, /testID="artifact-video-problem-close" onPress=\{close\}/)
  assert.match(workspaceFileViewer, /<SheetCloseButton onPress=\{onClose\} label="Close workspace files" testID="workspace-file-viewer-close" activateOnPressIn/)
  assert.match(workspaceFileViewer, /onAccessibilityEscape=\{onClose\}[\s\S]*?modalPadding/)
  assert.match(workspaceFileViewer, /collapsable=\{false\} pointerEvents="auto" style=\{\[styles\.header/)
  assert.match(workspaceFileViewer, /header: \{ minHeight: 64, flexShrink: 0, position: 'relative', zIndex: 20, elevation: 20/)
  assert.match(workspaceFileViewer, /preview: \{ flex: 1, minWidth: 0, minHeight: 0, overflow: 'hidden' \}/)
  assert.match(workspaceFileViewer, /testID="workspace-file-viewer-panes"[\s\S]*?testID="workspace-file-viewer-browser-pane"[\s\S]*?testID="workspace-file-viewer-preview-pane"/)
  assert.match(workspaceFileViewer, /mobileFileViewerPaneVisibility\(layout, Boolean\(selected\)\)/)
  assert.doesNotMatch(workspaceFileViewer, /\{layout === 'pad' \? <View style=\{styles\.padBody\}/)
  assert.match(fullscreenImageViewer, /width: 48, height: 48/)
  assert.match(fullscreenImageViewer, /Gesture\.Pinch\(\)[\s\S]*?pinchStartScale\.value = scale\.value[\s\S]*?clampImageViewerScale/)
  assert.match(fullscreenImageViewer, /Gesture\.Pan\(\)[\s\S]*?minPointers\(1\)[\s\S]*?maxPointers\(1\)[\s\S]*?panOwnsZoom\.value = imageViewerIsZoomed\(scale\.value\)/)
  assert.match(fullscreenImageViewer, /Gesture\.Simultaneous\(pinchGesture, panGesture\)/)
  assert.match(fullscreenImageViewer, /\.onUpdate\(event => \{[\s\S]*?if \(pinchActive\.value\)[\s\S]*?Pinch exclusively owns transforms/)
  assert.match(fullscreenImageViewer, /\.onEnd\(event => \{[\s\S]*?if \(pinchActive\.value\)[\s\S]*?panOwnsZoom\.value \|\| imageViewerIsZoomed\(scale\.value\)/)
  assert.match(fullscreenImageViewer, /cancelsTouchesInView\(false\)/)
  assert.match(fullscreenImageViewer, /dismissEnabled && shouldDismissImageViewer\(event\.translationX, event\.translationY, event\.velocityY\)[\s\S]*?runOnJS\(dismiss\)\(\)/)
  assert.match(fullscreenImageViewer, /testID=\{`\$\{testID\}-zoom-out`\}[\s\S]*?testID=\{`\$\{testID\}-zoom-reset`\}[\s\S]*?testID=\{`\$\{testID\}-zoom-in`\}/)
  assert.match(fullscreenImageViewer, /useEffect\(\(\) => \{[\s\S]*?resetZoom\(false\)[\s\S]*?\}, \[resetKey, resetZoom\]\)/)
  assert.match(fullscreenImageViewer, /<View onLayout=\{handleLayout\}[\s\S]*?<GestureDetector gesture=\{imageGesture\}>[\s\S]*?<View pointerEvents="box-none" style=\{styles\.zoomControlsPosition\}>/)
  assert.doesNotMatch(fullscreenImageViewer, /\.onUpdate\([\s\S]{0,800}?setCommittedScale/)
  assert.match(imageViewerGesture, /IMAGE_VIEWER_DISMISS_DISTANCE = 96/)
  assert.match(imageViewerGesture, /IMAGE_VIEWER_DISMISS_FLING_DISTANCE = 24/)
  assert.match(imageViewerGesture, /IMAGE_VIEWER_DISMISS_VELOCITY = 850/)
  assert.match(imageViewerGesture, /IMAGE_VIEWER_MIN_SCALE = 1/)
  assert.match(imageViewerGesture, /IMAGE_VIEWER_MAX_SCALE = 4/)
  assert.match(imageViewerGesture, /function clampImageViewerTranslation/)
  assert.match(filePreview, /kind === 'image'[\s\S]*?<SwipeDismissImage[\s\S]*?resetKey=\{activePreviewURL\}[\s\S]*?<Image/)
  assert.match(composer, /<SwipeDismissImage onDismiss=\{closePreview\} resetKey=\{preview\.source\.uri\}/)
  const videoDismissSurface = fullscreenImageViewer.slice(
    fullscreenImageViewer.indexOf('export function SwipeDismissVideoSurface('),
    fullscreenImageViewer.indexOf('export function FullscreenViewerCloseButton('),
  )
  assert.match(videoDismissSurface, /Gesture\.Pan\(\)[\s\S]*?activeOffsetY\(8\)[\s\S]*?failOffsetY\(-12\)[\s\S]*?failOffsetX\(\[-96, 96\]\)/)
  assert.match(videoDismissSurface, /cancelsTouchesInView\(false\)/)
  assert.match(videoDismissSurface, /shouldDismissImageViewer\(event\.translationX, event\.translationY, event\.velocityY\)[\s\S]*?runOnJS\(onDismiss\)\(\)/)
  assert.match(videoDismissSurface, /<View collapsable=\{false\} testID=\{testID\} style=\{style\} \/>/)
  assert.doesNotMatch(videoDismissSurface, /Animated|translate|transform|useSharedValue/)
  assert.match(artifactFileViewer, /header: \{ minHeight: 64/)
  assert.match(artifactFileViewer, /layout === 'pad'[\s\S]*?showFileRail[\s\S]*?<ArtifactSidebar/)
  assert.match(artifactFileViewer, /layout === 'phone'[\s\S]*?<PhoneNavigation/)
})

test('workspace file editing is revision-safe, keyboard-safe, and protects dirty drafts', () => {
  assert.match(agentServerClient, /workspaceWriteFile\(sessionId: string, path: string, content: string, expectedRevision: string\): Promise<WorkspaceFile>/)
  assert.match(agentServerClient, /expected_revision: expectedRevision/)
  assert.match(agentServerClient, /if \(error\.status !== 401\) return/)
  assert.match(workspaceFileViewer, /workspaceWriteFile\(request\.sessionId, path, content, expectedRevision\)/)
  assert.match(workspaceFileViewer, /const sessionCurrent = selectedSessionId === request\.sessionId[\s\S]*?sessionCurrent[\s\S]*?<ScopedWorkspaceFileViewer/)
  assert.match(workspaceFileViewer, /Your open file and unsaved edits are preserved/)
  assert.match(workspaceFileViewer, /if \(!workspaceConnectionIsCurrent\(connection\)\) throw new Error\('Reconnect this server before saving the file\.'\)[\s\S]*?workspaceWriteFile/)
  assert.match(workspaceFileViewer, /workspaceWriteFile[\s\S]*?if \(!workspaceConnectionIdentityIsCurrent\(connection\)\)/)
  assert.match(workspaceFileViewer, /function validatedWorkspaceText[\s\S]*?file\.path !== expectedPath/)
  assert.equal(workspaceFileViewer.includes('/^[a-f0-9]{64}$/i.test(file.revision)'), true)
  assert.match(workspaceFileViewer, /file\.size !== contentBytes/)
  assert.match(workspaceFileViewer, /currentInfo\.read_only \|\| currentInfo\.capability_version < 2/)
  assert.match(workspaceFileViewer, /layout === 'pad' \? 8 \* 1024 \* 1024 : 2 \* 1024 \* 1024/)
  assert.match(workspaceFileViewer, /<KeyboardAvoidingView[\s\S]*?keyboardVerticalOffset=\{Platform\.OS === 'ios' \? modalPadding\.paddingTop : 0\}/)
  assert.doesNotMatch(workspaceFileViewer, /from 'react-native-keyboard-controller'/)
  assert.match(workspaceFileViewer, /phoneActions: \{ minHeight: 52, flexShrink: 0, borderTopWidth:/)
  assert.match(workspaceFileViewer, /breadcrumb: \{ minHeight: 44/)
  assert.match(workspaceFileViewer, /Discard unsaved changes\?[\s\S]*?Keep Editing[\s\S]*?Discard[\s\S]*?Save/)
  assert.match(filePreview, /textRef\.current = value[\s\S]*?setText\(value\)[\s\S]*?updateDraft\(value\.content\)[\s\S]*?setEditing\(workspaceTextIsDirectlyEditable\(value, Boolean\(saveText\), effectiveTextLimit\)\)/)
  assert.match(filePreview, /import \{ utf8ByteLength, workspaceTextIsDirectlyEditable \}/)
  assert.doesNotMatch(filePreview, /testID="workspace-file-edit"/)
  assert.match(filePreview, /testID="workspace-file-edit-revert"/)
  assert.match(filePreview, /testID="workspace-file-edit-save"/)
  assert.match(filePreview, /testID="workspace-file-editor"/)
  assert.match(filePreview, /setText\(saved\)[\s\S]*?setEditing\(workspaceTextIsDirectlyEditable\(saved, Boolean\(saveText\), effectiveTextLimit\)\)/)
  assert.match(filePreview, /ref=\{editorRef\}/)
  assert.match(filePreview, /blur: blurEditor/)
  assert.match(filePreview, /draftRef\.current = value[\s\S]*?const authoritative = textRef\.current[\s\S]*?setDraftState[\s\S]*?onDirtyChange\?\.\(nextDirty\)/)
  assert.match(filePreview, /<MobileCodeEditor[\s\S]*?onChange=\{\(value, metadata\) => updateDraft\(value, metadata\)\}/)
  assert.match(filePreview, /const plainTextEditor = <TextInput[\s\S]*?onChangeText=\{updateDraft\}/)
  assert.match(filePreview, /^import \{ MobileCodeEditor, type MobileCodeEditorController, type MobileCodeEditorSnapshot \} from '\.\.\/\.\.\/editor\/MobileCodeEditor'/m)
  assert.doesNotMatch(filePreview, /lazy\(\(\) => import\('\.\.\/\.\.\/editor\/MobileCodeEditor'\)/)
  assert.doesNotMatch(filePreview, /<Suspense/)
  assert.match(filePreview, /renderFallback=\{\(\) => plainTextEditor\}/)
  assert.match(filePreview, /const editorReadyRef = useRef\(false\)/)
  assert.match(filePreview, /const handleEditorStatus = useCallback\(\(status: \{ fallback: boolean; ready: boolean \}\) => \{\s+editorReadyRef\.current = status\.ready/)
  assert.match(filePreview, /catch \(cause\) \{\s+const latestSnapshot = acceptEditorSnapshot\(controller\.getSnapshot\(\)\)[\s\S]*?if \(!editorReadyRef\.current && !latestSnapshot\.dirty && !hasUnsavedChanges\(\)\) return true[\s\S]*?onDirtyChange\?\.\(true\)\s+return false/)
  assert.match(filePreview, /hasUnsavedChanges,[\s\S]*?hasNewerDraftSinceSave,[\s\S]*?isSaving,/)
  assert.match(filePreview, /canSave: canSaveCurrentDraft/)
  assert.match(filePreview, /const effectiveTextLimit = useRef\(Math\.min\(textLimit, serverTextLimit\)\)\.current/)
  assert.match(filePreview, /afterEditorBlur\(\(\) => Alert\.alert\('Discard unsaved changes\?'/)
  assert.match(workspaceFileViewer, /const commitClose = useCallback\(\(\) => \{\s+editController\.current\?\.blur\(\)\s+dismissAppKeyboard\(\)\s+onClose\(\)/)
  assert.match(workspaceFileViewer, /requestAnimationFrame\(\(\) => \{[\s\S]*?const controller = editController\.current[\s\S]*?const departure = workspaceFileDepartureDecision/)
  assert.match(workspaceFileViewer, /prepareWorkspaceEditorForDeparture\(editController\.current\)\.then\(prepared => \{[\s\S]*?!prepared \|\| dirtyRef\.current/)
  assert.match(workspaceFileViewer, /prepareWorkspaceEditorForDeparture\(editController\.current\)\.then\(prepared => \{[\s\S]*?!prepared \|\| hasUnsavedEditsRef\.current/)
  assert.match(workspaceFileViewer, /const prepared = await controller\.prepareForDeparture\(\)[\s\S]*?return prepared[\s\S]*?catch[\s\S]*?return false/)
  assert.match(workspaceFileViewer, /hasNewerDraftSinceSave\(\)[\s\S]*?departure\.kind === 'confirm_saving'[\s\S]*?Close Anyway/)
  assert.match(workspaceFileViewer, /!infoResolved[\s\S]*?Checking file access…[\s\S]*?saveText=\{info && !info\.read_only && info\.capability_version >= 2 \? saveSelectedText : undefined\}/)
  assert.match(workspaceFileViewer, /Could not check file access[\s\S]*?accessibilityLabel="Retry file access"/)
  assert.match(workspaceFileViewer, /label="Back to workspace files" activateOnPressIn/)
  assert.match(filePreview, /const dirty = draftState\.dirty/)
  assert.match(filePreview, /saveText\(submittedDraft, authoritative\.revision\)/)
  assert.match(filePreview, /const hasNewerDraft = draftRef\.current !== submittedDraft[\s\S]*?onTextSaved\?\.\(saved, hasNewerDraft\)/)
  assert.match(filePreview, /const editorDocumentValueRef = useRef\(''\)[\s\S]*?value=\{editorDocumentValueRef\.current\}/)
  assert.match(filePreview, /if \(!hasNewerDraft\) \{\s+editorDocumentValueRef\.current = saved\.content[\s\S]*?editorRef\.current\?\.markClean\(saved\.content\)/)
  assert.match(workspaceFileViewer, /setViewerDirty\(hasNewerDraft\)/)
  assert.match(filePreview, /status === 409 \|\| code === 'workspace_file_conflict'/)
  assert.match(filePreview, /!saveRequiresReload[\s\S]*?!currentDraft\.includes\('\\0'\)[\s\S]*?utf8ByteLength\(currentDraft\) <= effectiveTextLimit/)
  assert.match(filePreview, /disabled=\{!canSaveCurrentDraft\(\)\}/)
  assert.match(filePreview, /draftValidationError = dirty && draftState\.hasNull[\s\S]*?draftState\.utf8Bytes > effectiveTextLimit[\s\S]*?Draft exceeds the \$\{formatLimit\(effectiveTextLimit\)\} editing limit/)
  assert.match(filePreview, /saveError \|\| draftValidationError \|\| \(dirty \? 'Unsaved changes'/)
  assert.match(workspaceFileViewer, /controller\?\.canSave\(\) \? \[\{ text: 'Save'/)
  assert.match(filePreview, /Your edits are safe; reload before saving again/)
  assert.match(filePreview, /submittedDraft\.includes\('\\0'\)/)
  assert.match(workspaceFileEditing, /new TextEncoder\(\)\.encode\(value\)\.byteLength/)
  assert.match(mobileCodeEditor, /const mirrorRef = useAuthoritativeMirrorRef\(value, path, initialViewState\)/)
  assert.match(mobileCodeEditor, /const stripped = renderStateFromMirror\(next\)[\s\S]*?requestAnimationFrame\(\(\) => \{[\s\S]*?setRenderState\(pending\)/)
  assert.match(mobileCodeEditor, /onContentProcessDidTerminate=\{\(\) => \{\s+if \(webViewKey === activeWebViewGenerationRef\.current\) startRecovery/)
  assert.match(mobileCodeEditor, /originWhitelist=\{\['about:blank', 'https:\/\/agentsdock\.local'\]\}/)
  assert.match(codeEditorEngine, /EditorState\.transactionFilter[\s\S]*?attemptedBytes > maxBytes/)
  assert.match(codeEditorEngine, /searchOccurrenceCounter/)
  assert.match(codeEditorAssets, /default-src 'none'/)
})

test('mobile editor initialization survives a missed ready event and cannot load forever', () => {
  assert.match(mobileCodeEditor, /const INITIALIZATION_TIMEOUT_MS = 6_000/)
  assert.match(mobileCodeEditor, /const MAX_INITIALIZATION_RECOVERY_ATTEMPTS = 1/)
  assert.match(mobileCodeEditor, /const sendInitialize = useCallback[\s\S]*?engineInitializedRef\.current \|\| initializationSentRef\.current[\s\S]*?postCommand\(documentCommand\('initialize'\)/)
  assert.match(mobileCodeEditor, /if \(eventType === 'ready'\) \{[\s\S]*?sendInitialize\(\)/)
  assert.match(mobileCodeEditor, /activeWebViewGenerationRef\.current = nextGeneration\s+setWebViewKey\(nextGeneration\)/)
  assert.match(mobileCodeEditor, /const armInitializationTimeout = useCallback\(\(generation: number\) => \{\s+if \(generation !== activeWebViewGenerationRef\.current\) return[\s\S]*?setTimeout\([\s\S]*?generation !== activeWebViewGenerationRef\.current/)
  assert.match(mobileCodeEditor, /useEffect\(\(\) => \{\s+armInitializationTimeout\(webViewKey\)\s+return clearInitializationTimeout\s+\}, \[armInitializationTimeout, clearInitializationTimeout, webViewKey\]\)/)
  assert.match(mobileCodeEditor, /onLoadEnd=\{\(\) => \{\s+if \(webViewKey !== activeWebViewGenerationRef\.current\) return\s+sendInitialize\(\)\s+armInitializationTimeout\(webViewKey\)/)
  assert.match(mobileCodeEditor, /onMessage=\{event => handleEngineMessage\(event, webViewKey\)\}/)
  assert.match(mobileCodeEditor, /recoveryAttemptsRef\.current >= MAX_INITIALIZATION_RECOVERY_ATTEMPTS[\s\S]*?enterFallback\(reason\)[\s\S]*?recoveryHandlerRef\.current/)
  assert.match(mobileCodeEditor, /if \(eventType === 'initialized'\) \{\s+clearInitializationTimeout\(\)[\s\S]*?engineInitializedRef\.current = true/)
  assert.match(mobileCodeEditor, /const retry = useCallback\(\(\) => \{\s+clearInitializationTimeout\(\)[\s\S]*?initializationSentRef\.current = false/)
  assert.match(mobileCodeEditor, /useEffect\(\(\) => \(\) => \{\s+disposedRef\.current = true\s+clearInitializationTimeout\(\)/)
  assert.match(mobileCodeEditor, /accessible\s+accessibilityRole="progressbar"[\s\S]*?accessibilityLiveRegion="polite"[\s\S]*?accessibilityState=\{\{ busy: true \}\}[\s\S]*?pointerEvents="auto"/)
  assert.doesNotMatch(mobileCodeEditor, /phase === 'loading'[\s\S]{0,160}pointerEvents="none"/)
})

test('workspace mutations are capability-gated, revision-fenced, and adaptive', () => {
  assert.match(workspaceFileViewer, /const mutationsAvailable = Boolean\(info && info\.capability_version >= 6 && !info\.read_only\)/)
  assert.match(workspaceFileViewer, /workspaceCreateEntry\(request\.sessionId, path, kind\)/)
  assert.match(workspaceFileViewer, /workspaceRenameEntry\(request\.sessionId, previousPath, name, entry\.revision\)/)
  assert.match(workspaceFileViewer, /workspaceRemoveEntry\(request\.sessionId, path, entry\.revision, entry\.kind === 'directory'\)/)
  assert.match(workspaceFileViewer, /if \(!validWorkspaceEntryRevision\(entry\.revision\)\)[\s\S]*?Refresh the file explorer before renaming this entry/)
  assert.match(workspaceFileViewer, /if \(!validWorkspaceEntryRevision\(entry\.revision\)\)[\s\S]*?Refresh the file explorer before deleting this entry/)
  assert.match(workspaceFileViewer, /if \(kind === 'file'\) confirmDiscard\(create\)/)
  assert.match(workspaceFileViewer, /const protectAffectedEditor[\s\S]*?workspacePathIsWithin\(selectedPath, entry\.path\)\) confirmDiscard\(action\)/)
  assert.match(workspaceFileViewer, /This recursively deletes the folder and everything inside it\. This cannot be undone\./)
  assert.match(workspaceFileViewer, /selectedRef\.current = created[\s\S]*?setSelected\(created\)[\s\S]*?setPreviewRevision/)
  assert.match(workspaceFileViewer, /setEntries\(current => current\.map\(entry => entry\.path === path \? \{ \.\.\.entry, revision: saved\.revision/)
  assert.match(workspaceFileViewer, /selectedRef\.current = updatedSelected[\s\S]*?setSelected\(updatedSelected\)/)
  assert.match(workspaceFileViewer, /renameWorkspaceEntry\(latestWorkspaceMutationEntry\(entry\), value\)/)
  assert.match(workspaceFileViewer, /removeWorkspaceEntry\(latestWorkspaceMutationEntry\(entry\)\)/)
  assert.match(workspaceFileViewer, /refreshAfterWorkspaceMutation\(targetDirectory, true\)/)
  assert.match(workspaceFileViewer, /layout === 'phone'[\s\S]*?testID="workspace-phone-actions"/)
  assert.match(workspaceFileViewer, /layout === 'phone'[\s\S]*?testID=\{`workspace-entry-more-\$\{entry\.path\}`\}/)
  assert.match(workspaceFileViewer, /layout === 'pad'[\s\S]*?testID="workspace-create-file"[\s\S]*?testID="workspace-create-folder"/)
  assert.match(workspaceFileViewer, /layout === 'pad'[\s\S]*?testID=\{`workspace-entry-rename-\$\{entry\.path\}`\}[\s\S]*?testID=\{`workspace-entry-delete-\$\{entry\.path\}`\}/)
  assert.match(workspaceFileViewer, /icon=\{Pencil\} size=\{15\} touchSize=\{44\}/)
  assert.match(workspaceFileViewer, /icon=\{Trash2\} size=\{15\} touchSize=\{44\}/)
  assert.match(workspaceFileViewer, /accessibilityRole=\{mutationError \? 'alert' : undefined\}[\s\S]*?testID="workspace-mutation-error-dismiss"/)
})

test('compact chat status remains readable and touch-safe', () => {
  assert.match(chatHeader, /\{!compact \? <IconButton icon=\{RefreshCw\}/)
  assert.match(chatHeader, /<Text style=\{\[styles\.statusLabel/)
  assert.match(chatHeader, /online: \{ minHeight: 44/)
  assert.match(chatHeader, /onlineCompact: \{ minWidth: 78/)
  assert.match(chatHeader, /statusLabel: \{ fontSize: 11, fontWeight: '700', flexShrink: 0/)
  assert.match(chatHeader, /spinner: \{ width: 20, height: 20/)
  assert.doesNotMatch(chatHeader, /scale: 0\.65/)
})

test('native terminal relies on SwiftTerm tap handling without a competing recognizer', () => {
  assert.doesNotMatch(terminal, /UITapGestureRecognizer/)
  assert.doesNotMatch(terminal, /focusFromTap/)
})

test('foregrounding and hidden views cannot resurrect a phantom keyboard', () => {
  const readyHandler = terminal.match(/case "ready":([\s\S]*?)case "error":/)?.[1]
  assert.ok(readyHandler)
  assert.doesNotMatch(readyHandler, /focusTerminal|becomeFirstResponder/)
  assert.match(terminal, /public override func willMove\(toWindow newWindow: UIWindow\?\)[\s\S]*?newWindow == nil[\s\S]*?blurTerminal/)
  assert.match(terminal, /UIApplication\.willResignActiveNotification/)
  assert.match(terminal, /public func focusTerminal\(\) -> Bool \{[\s\S]*?applicationState == \.active/)
  assert.match(terminal, /public func blurTerminal\(\) -> Bool/)
  assert.match(terminalModule, /AsyncFunction\("blur"\)/)
  assert.equal((terminalModule.match(/\.runOnQueue\(\.main\)/g) ?? []).length, 4)
  assert.match(terminalTypes, /blur\(\): Promise<boolean>/)
  assert.match(terminalView, /nativeTerminal\.blur\(\)\.catch/)

  assert.match(appDelegateSubscriber, /applicationWillResignActive[\s\S]*?connectedScenes[\s\S]*?endEditing\(true\)/)
  assert.match(appDelegateSubscriber, /var windows = application\.windows/)
  assert.match(appDelegateSubscriber, /private var needsForegroundCleanup = false/)
  assert.match(appDelegateSubscriber, /applicationDidBecomeActive[\s\S]*?guard needsForegroundCleanup else \{ return \}/)
  assert.match(appDelegateSubscriber, /applicationDidBecomeActive[\s\S]*?DispatchQueue\.main\.async[\s\S]*?application\.applicationState == \.active[\s\S]*?endEditing\(in: application\)/)
  assert.match(terminalModuleConfig, /"appDelegateSubscribers": \["AgentsDockKeyboardAppDelegateSubscriber"\]/)
  assert.match(appKeyboard, /AppState\.addEventListener\('change'/)
  assert.match(appKeyboard, /Keyboard\.dismiss\(\)/)
  assert.match(appKeyboard, /returningToForeground[\s\S]*?setTimeout[\s\S]*?dismissAppKeyboard\(\)/)
  assert.match(appKeyboard, /clearTimeout\(resumeTimer\)[\s\S]*?subscription\.remove\(\)/)
  assert.match(appShell, /useAppKeyboardLifecycle\(\)/)
  assert.match(appShell, /const openMobileChat[\s\S]*?setMobileChatOpen\(true\)[\s\S]*?requestAnimationFrame\(dismissAppKeyboard\)/)
  assert.match(sidebar, /ref=\{searchInput\}/)
  assert.match(sidebar, /searchInput\.current\?\.blur\(\)/)
  assert.doesNotMatch(chatScreen, /KeyboardController\.dismiss/)
  assert.doesNotMatch(chatScreen, /setKeyboardVisible\(KeyboardController\.isVisible\(\)\)/)
  assert.match(chatScreen, /enabled=\{Platform\.OS !== 'ios' \|\| iosKeyboard\.avoidanceEnabled\}/)
  assert.match(keyboardControllerPatch, /if \(behavior === "padding"\) \{[\s\S]*?return \{ paddingBottom: 0 \};/)
  assert.match(chatScreen, /dispatchIOSKeyboard\(\{ type: 'app-state', status: state \}\)/)
  assert.match(keyboardLifecycle, /event\.status !== 'active'[\s\S]*?visible: false[\s\S]*?avoidanceEnabled: false[\s\S]*?hideCompletionPending: false/)
  assert.match(chatScreen, /if \(state !== 'active'\) cancelHideFallback\(\)/)
  assert.match(chatScreen, /hideFallbackTimer = setTimeout\([\s\S]*?keyboard-hide-timeout[\s\S]*?IOS_KEYBOARD_HIDE_FALLBACK_MS/)
  assert.match(chatScreen, /cancelHideFallback\(\)[\s\S]*?dispatchIOSKeyboard\(\{ type: 'keyboard-will-show' \}\)/)
  assert.match(keyboardLifecycle, /keyboard-will-hide[\s\S]*?visible: false[\s\S]*?keyboard-did-hide[\s\S]*?avoidanceEnabled: false/)
  assert.match(keyboardLifecycle, /keyboard-hide-timeout[\s\S]*?hideCompletionPending[\s\S]*?avoidanceEnabled: false/)
  assert.match(keyboardLifecycle, /keyboard-did-show[\s\S]*?hideCompletionPending[\s\S]*?visible: true[\s\S]*?avoidanceEnabled: true/)
  assert.match(dialogs, /onShow=\{\(\) => searchInput\.current\?\.focus\(\)\}/)
  assert.doesNotMatch(dialogs, /<TextInput autoFocus value=\{query\}/)
})

test('timeline preserves ordinary interaction and completes a quick keyboard pull-down', () => {
  assert.match(timeline, /keyboardDismissMode="interactive"/)
  assert.match(timeline, /keyboardShouldPersistTaps="always"/)
  assert.match(keyboardGesture, /TIMELINE_KEYBOARD_DISMISS_VELOCITY = 0\.2/)
  assert.match(keyboardGesture, /keyboardWasVisible[\s\S]*?velocityY <= -TIMELINE_KEYBOARD_DISMISS_VELOCITY/)
  assert.match(timeline, /keyboardBottomAnchor\.current = !userScrolling\.current && nearBottomRef\.current/)
  const beginDrag = timeline.slice(
    timeline.indexOf('const handleScrollBeginDrag = useCallback'),
    timeline.indexOf('const handleScrollEndDrag = useCallback'),
  )
  assert.match(beginDrag, /event\.nativeEvent\.contentOffset\.y[\s\S]*?keyboardAnchorToken\.current \+= 1[\s\S]*?dragStartedWithKeyboard\.current = keyboardVisible[\s\S]*?userScrolling\.current = true[\s\S]*?topLoadArmed\.current = !loadingOlder/)
  assert.doesNotMatch(beginDrag, /Keyboard\.dismiss\(\)/)
  const endDrag = timeline.slice(
    timeline.indexOf('const handleScrollEndDrag = useCallback'),
    timeline.indexOf('const handleMomentumScrollBegin = useCallback'),
  )
  assert.match(endDrag, /Platform\.OS === 'ios'[\s\S]*?shouldFinishTimelineKeyboardDismissal\(dragStartedWithKeyboard\.current, event\.nativeEvent\.velocity\?\.y\)[\s\S]*?Keyboard\.dismiss\(\)/)
  assert.match(timeline, /onScrollBeginDrag=\{handleScrollBeginDrag\}/)
  assert.match(timeline, /onScrollEndDrag=\{handleScrollEndDrag\}/)
  assert.match(timeline, /\[activeHistoryWindow\?\.detached, keyboardSettleRequest\]/)
  assert.match(timeline, /keyboardAnchorToken\.current === anchorToken[\s\S]*?scrollToEnd\(\{ animated: false \}\)/)

  assert.match(chatScreen, /Keyboard\.addListener\('keyboardDidShow',[\s\S]*?setKeyboardSettleRequest/)
  assert.match(chatScreen, /Keyboard\.addListener\('keyboardDidHide',[\s\S]*?setKeyboardSettleRequest/)
  assert.match(chatScreen, /behavior=\{Platform\.OS === 'ios' \? 'padding' : Platform\.OS === 'android' \? 'height' : undefined\}/)
  assert.match(chatScreen, /keyboardVerticalOffset=\{Platform\.OS === 'ios' \? insets\.top : 0\}/)
  assert.match(chatScreen, /automaticOffset=\{Platform\.OS === 'android'\}/)
  assert.match(chatScreen, /paddingBottom: Platform\.OS === 'ios'[\s\S]*?\? iosKeyboard\.avoidanceEnabled \? 0 : insets\.bottom/)
})

test('Android video preview retains bounded Expo Video playback controls', () => {
  const videoPlayer = artifactNativeVideoPlayer.slice(
    artifactNativeVideoPlayer.indexOf('export function ArtifactNativeVideoPlayer('),
    artifactNativeVideoPlayer.indexOf('\nfunction ArtifactVideoTimeline('),
  )
  const videoTimeline = artifactNativeVideoPlayer.slice(
    artifactNativeVideoPlayer.indexOf('function ArtifactVideoTimeline('),
    artifactNativeVideoPlayer.indexOf('\nconst styles = StyleSheet.create'),
  )
  assert.equal(mobilePackage.dependencies['expo-video'], '~57.0.2')
  assert.match(artifactPlatformVideoPlayerAndroid, /<ArtifactNativeVideoPlayer \{\.\.\.props\}/)
  assert.doesNotMatch(videoPlayer, /GestureDetector|Gesture\.Pinch\(|Animated\.View|useSharedValue|\btransform\s*:/)
  assert.match(artifactFileViewer, /presentationStyle="fullScreen"/)
  assert.match(videoPlayer, /const customControls = Platform\.OS === 'ios' && layout === 'phone'/)
  assert.match(videoPlayer, /const player = useVideoPlayer\(null/)
  assert.match(videoPlayer, /player\.replaceAsync\(source\)/)
  assert.match(artifactNativeVideoPlayer, /const VIDEO_LOAD_TIMEOUT_MS = 45_000/)
  assert.match(videoPlayer, /nativeControls=\{!customControls && ready\}/)
  assert.match(videoPlayer, /fullscreenOptions=\{\{ enable: !customControls && Platform\.OS !== 'android' \}\}/)
  assert.match(videoPlayer, /allowsVideoFrameAnalysis=\{false\}/)
  assert.match(videoPlayer, /contentFit=\{videoFit\}/)
  assert.match(videoPlayer, /setVideoFit\('contain'\)[\s\S]*?player\.replaceAsync\(source\)/)
  assert.match(videoPlayer, /surfaceType=\{Platform\.OS === 'android' \? 'textureView' : undefined\}/)
  assert.match(videoPlayer, /testID="artifact-video-zoom-out"[\s\S]*?setVideoFit\('contain'\)/)
  assert.match(videoPlayer, /testID="artifact-video-zoom-in"[\s\S]*?setVideoFit\('cover'\)/)
  assert.match(videoPlayer, /value\.timeUpdateEventInterval = 0/)
  assert.match(videoPlayer, /value\.staysActiveInBackground = false/)
  assert.match(videoPlayer, /value\.bufferOptions = \{ preferredForwardBufferDuration: 8, waitsToMinimizeStalling: true \}/)
  assert.match(videoPlayer, /player\.timeUpdateEventInterval = customControls && appActive && isPlaying \? 0\.5 : 0/)
  assert.match(videoPlayer, /NativeAppState\.addEventListener\('change'[\s\S]*?if \(!active\) pause\(\)/)
  assert.doesNotMatch(videoPlayer, /useEffect\(\(\) => \(\) => \{[\s\S]*?player\.pause\(\)/)
  assert.match(videoPlayer, /const pause = useCallback\(\(\) => \{[\s\S]*?try \{ player\.pause\(\) \}[\s\S]*?return registerPause\(pause\)/)
  assert.match(videoPlayer, /testID="artifact-video-problem"/)
  assert.match(videoPlayer, /testID="artifact-video-retry"[\s\S]*?testID="artifact-video-download"[\s\S]*?testID="artifact-video-problem-close"/)
  assert.match(artifactFileViewer, /if \(activeVideoPauseRef\.current === pause\) activeVideoPauseRef\.current = null/)
  assert.match(artifactFileViewer, /if \(!nextId \|\| nextId === file\?\.id \|\| navigationInFlightRef\.current\) return[\s\S]*?activeVideoPauseRef\.current\?\.\(\)[\s\S]*?setCurrentId\(nextId\)/)
  assert.match(videoPlayer, /<SwipeDismissVideoSurface[\s\S]*?onDismiss=\{close\}/)
  assert.match(videoPlayer, /<ArtifactVideoTimeline[\s\S]*?currentTime=\{timeUpdate\.currentTime\}[\s\S]*?duration=\{duration\}/)
  assert.match(videoTimeline, /testID="artifact-video-timeline"/)
  assert.match(videoTimeline, /testID="artifact-video-timeline-track"[\s\S]*?\{\.\.\.responder\.panHandlers\}/)
  assert.match(videoTimeline, /onPanResponderGrant: begin[\s\S]*?onPanResponderMove: updateDraft[\s\S]*?onPanResponderRelease: finish/)
  assert.match(videoTimeline, /if \(value != null\) onSeek\(clampVideoTime\(value, validDuration\)\)/)
  assert.match(artifactNativeVideoPlayer, /testID="artifact-video-playback-toggle"/)
})

test('iOS video opening stages locally and uses an app-owned AVPlayer view instead of Expo Video', () => {
  assert.equal(mobilePackage.dependencies['agentsdock-native-video'], 'file:modules/agentsdock-native-video')
  assert.deepEqual(JSON.parse(nativeVideoModuleConfig).platforms, ['apple'])
  assert.match(artifactVideoPlayer, /if \(Platform\.OS !== 'ios'\)[\s\S]*?<ArtifactPlatformVideoPlayer/)
  assert.match(artifactVideoPlayer, /FileSystem\.createDownloadResumable\(/)
  assert.match(artifactVideoPlayer, /task\.cancelAsync\(\)/)
  assert.match(artifactVideoPlayer, /IOS_VIDEO_STAGE_STALL_MS/)
  assert.match(artifactVideoPlayer, /completedArtifactVideoDownload\(downloaded, expectedBytes\)/)
  assert.match(artifactVideoPlayer, /localURI && mountPlayer[\s\S]*?<ArtifactIOSSystemVideoPlayer[\s\S]*?sourceURI=\{localURI\}/)
  assert.doesNotMatch(artifactVideoPlayer, /ArtifactNativeVideoPlayer|from 'expo-video'/)
  assert.match(artifactVideoPlayer, /setTimeout\(\(\) => setMountPlayer\(true\), IOS_VIDEO_STAGE_PLAYER_DELAY_MS\)/)
  assert.match(artifactVideoPlayer, /testID="artifact-video-stage-close" onPress=\{close\}/)
  assert.match(artifactVideoPlayer, /accessibilityRole="progressbar"/)
  assert.match(artifactIOSSystemVideoPlayer, /useMemo\(requireAgentsDockNativeVideo, \[\]\)/)
  assert.match(artifactIOSSystemVideoPlayer, /const IOS_NATIVE_VIDEO_LOAD_TIMEOUT_MS = 15_000/)
  assert.match(artifactIOSSystemVideoPlayer, /registerPause\(pause\)/)
  assert.match(artifactIOSSystemVideoPlayer, /testID="artifact-ios-system-video-retry"[\s\S]*?testID="artifact-ios-system-video-download"[\s\S]*?testID="artifact-ios-system-video-close"/)
  assert.match(nativeVideoTypes, /function requireAgentsDockNativeVideo/)
  assert.match(nativeVideoTypes, /requireNativeViewManager<AgentsDockNativeVideoProps>/)
  assert.match(nativeVideoModule, /Name\("AgentsDockNativeVideo"\)/)
  assert.match(nativeVideoModule, /View\(AgentsDockNativeVideoView\.self\)/)
  assert.match(nativeVideoView, /private let playerLayer = AVPlayerLayer\(\)/)
  assert.match(nativeVideoView, /AgentsDockNativeVideoView: ExpoView, UIScrollViewDelegate/)
  assert.match(nativeVideoView, /private let videoScrollView = UIScrollView\(\)/)
  assert.match(nativeVideoView, /videoContentView\.layer\.addSublayer\(playerLayer\)/)
  assert.doesNotMatch(nativeVideoView, /videoContentView\.transform\s*=/)
  assert.match(nativeVideoView, /viewForZooming\(in scrollView: UIScrollView\)[\s\S]*?videoContentView/)
  assert.match(nativeVideoView, /videoScrollView\.minimumZoomScale = 1/)
  assert.match(nativeVideoView, /videoScrollView\.maximumZoomScale = 4/)
  assert.match(nativeVideoView, /accessibilityIdentifier(?::| =) "agentsdock-video-zoom-out"/)
  assert.match(nativeVideoView, /accessibilityIdentifier = "agentsdock-video-zoom-reset"/)
  assert.match(nativeVideoView, /accessibilityIdentifier(?::| =) "agentsdock-video-zoom-in"/)
  assert.match(nativeVideoView, /resetZoom\(animated: false\)[\s\S]*?clearPlayer\(\)/)
  assert.match(nativeVideoView, /resetZoom\(animated: Bool\)[\s\S]*?panGestureRecognizer\.isEnabled = false[\s\S]*?pinchGestureRecognizer\?\.isEnabled = false/)
  assert.match(nativeVideoView, /guard let url = URL\(string: value\), url\.isFileURL/)
  assert.match(nativeVideoView, /FileManager\.default\.isReadableFile/)
  assert.match(nativeVideoView, /private let playPauseButton = UIButton\(type: \.system\)/)
  assert.match(nativeVideoView, /private let timelineSlider = UISlider\(\)/)
  assert.match(nativeVideoView, /accessibilityIdentifier = "agentsdock-video-play-pause"/)
  assert.match(nativeVideoView, /accessibilityIdentifier = "agentsdock-video-timeline"/)
  assert.match(nativeVideoView, /VoiceOver adjusts UISlider/)
  assert.match(nativeVideoView, /else if autoplay && ready/)
  assert.doesNotMatch(nativeVideoView, /AVPlayerViewController/)
  assert.match(nativeVideoView, /AVPlayerItemFailedToPlayToEndTime/)
  assert.match(nativeVideoView, /UIApplication\.willResignActiveNotification/)
  assert.match(nativeVideoView, /playerLayer\.player\?\.replaceCurrentItem\(with: nil\)/)
  assert.match(mediaGrid, /shouldGenerateAutomaticVideoThumbnail\(Platform\.OS, viewerActive\)/)
  assert.match(mediaGrid, /if \(Platform\.OS === 'ios' \|\| !enabled \|\| failed\)/)
  assert.doesNotMatch(mediaGrid, /viewerActive \? \[\] : media\.filter\(isVideo\)/)
  assert.match(artifactFileViewer, /const closeViewer = useCallback\([\s\S]*?activeVideoPauseRef\.current\?\.\(\)[\s\S]*?onClose\(\)/)
  assert.match(artifactFileViewer, /onAccessibilityEscape=\{closeViewer\}[\s\S]*?<ViewerHeader[\s\S]*?onClose=\{closeViewer\}/)
})

test('cold file-viewer import graph defers editor and video evaluation behind recoverable boundaries', () => {
  assert.doesNotMatch(fileViewerHost, /^import .*ArtifactFileViewerModal/m)
  assert.doesNotMatch(fileViewerHost, /^import .*WorkspaceFileViewerModal/m)
  assert.match(fileViewerHost, /lazy\(\(\) => importWithDeadline\([\s\S]*?import\('\.\/ArtifactFileViewerModal'\)/)
  assert.match(fileViewerHost, /lazy\(\(\) => importWithDeadline\([\s\S]*?import\('\.\/WorkspaceFileViewerModal'\)/)
  assert.match(fileViewerHost, /<DeferredLoadBoundary[\s\S]*?fallback=\{<DeferredViewerUnavailable/)
  assert.match(fileViewerHost, /testID="deferred-file-viewer-close"/)
  assert.match(fileViewerHost, /function DeferredViewerLoading/)
  assert.match(fileViewerHost, /testID="deferred-file-viewer-loading-close"/)
  assert.doesNotMatch(fileViewerHost, /import \{[^}]*\bModal\b[^}]*\} from 'react-native'/)
  assert.match(fileViewerHost, /styles\.deferredOverlay/)
  assert.match(fileViewerHost, /deferredOverlay: \{ position: 'absolute', top: 0, right: 0, bottom: 0, left: 0/)
  assert.doesNotMatch(fileViewerHost, /codeEditorAssets|expo-video/)

  assert.match(filePreview, /^import \{ MobileCodeEditor, type MobileCodeEditorController, type MobileCodeEditorSnapshot \} from '\.\.\/\.\.\/editor\/MobileCodeEditor'/m)
  assert.doesNotMatch(filePreview, /lazy\(\(\) => import\('\.\.\/\.\.\/editor\/MobileCodeEditor'\)/)
  assert.doesNotMatch(mediaGrid, /^import\s+\{\s*(?:VideoView|useVideoPlayer)/m)
  assert.match(mediaGrid, /lazy\(\(\) => importWithDeadline\([\s\S]*?import\('\.\/VideoThumbnailLoader'\)[\s\S]*?6_000/)
  assert.match(mediaGrid, /<DeferredLoadBoundary[^>]*onError=\{rejectThumbnail\}/)
  assert.doesNotMatch(artifactFileViewer, /from 'expo-video'/)
  assert.match(artifactFileViewer, /^import \{ ArtifactVideoPlayer \} from '\.\/ArtifactVideoPlayer'/m)
  assert.doesNotMatch(artifactFileViewer, /lazy\(\(\) => import\('\.\/ArtifactVideoPlayer'\)/)
  assert.match(mediaGrid, /const \{ openArtifacts, viewerActive \} = useFileViewer\(\)/)
  assert.match(mediaGrid, /shouldGenerateAutomaticVideoThumbnail\(Platform\.OS, viewerActive\)[\s\S]*?media\.filter\(isVideo\)\.map\(file => file\.id\)[\s\S]*?: \[\]/)

  assert.match(deferredLoadBoundary, /getDerivedStateFromError/)
  assert.match(deferredLoadBoundary, /componentDidCatch\(\)[\s\S]*?this\.props\.onError\?\.\(\)/)
  assert.match(deferredLoadBoundary, /previous\.resetKey !== this\.props\.resetKey/)
  assert.match(deferredImport, /export function importWithDeadline/)
  assert.match(deferredImport, /setTimeout\([\s\S]*?did not load within/)
  assert.doesNotMatch(codeEditorAssets, /^export const CODE_EDITOR_SCRIPT/m)
  assert.doesNotMatch(embedCodeEditor, /export const CODE_EDITOR_SCRIPT/)
  assert.match(embedCodeEditor, /JSON\.stringify\(`\$\{htmlPrefix\}\$\{script\}\$\{htmlSuffix\}`\)/)
})
